//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ComponentKB.hh"
#include "smartTimedTaskTrigger.h"
//FIXME: implement logging
//#include "smartGlobalLogger.hh"

// the ace port-factory is used as a default port-mapping
#include "ComponentKBAcePortFactory.hh"

#include "KbEventServerEventTestHandler.hh"

// initialize static singleton pointer to zero
ComponentKB* ComponentKB::_componentKB = 0;

// constructor
ComponentKB::ComponentKB()
{
	std::cout << "constructor of ComponentKB\n";
	
	// set all pointer members to NULL
	//componentKBParams = NULL;
	//coordinationSlave = NULL;
	dummy = NULL;
	dummyTrigger = NULL;
	kbChainedEntriesEventClient = NULL;
	kbChainedEntriesEventClientInputTaskTrigger = NULL;
	kbChainedEntriesEventClientUpcallManager = NULL;
	kbChainedEntriesEventClientHandler = NULL;
	kbEventServer = NULL;
	kbEventServerEventTestHandler = NULL; 
	kbQuery = NULL;
	kbQueryInputTaskTrigger = NULL;
	kbQueryHandler = NULL;
	stateChangeHandler = NULL;
	stateSlave = NULL;
	wiringSlave = NULL;
	param = NULL;
	
	// set default ini parameter values
	connections.component.name = "ComponentKB";
	connections.component.initialComponentMode = "Neutral";
	connections.component.defaultScheduler = "DEFAULT";
	connections.component.useLogger = false;
	
	connections.kbEventServer.serviceName = "kbEventServer";
	connections.kbEventServer.roboticMiddleware = "ACE_SmartSoft";
	connections.kbQuery.serviceName = "kbQuery";
	connections.kbQuery.roboticMiddleware = "ACE_SmartSoft";
	connections.kbChainedEntriesEventClient.initialConnect = false;
	connections.kbChainedEntriesEventClient.wiringName = "kbChainedEntriesEventClient";
	connections.kbChainedEntriesEventClient.serverName = "unknown";
	connections.kbChainedEntriesEventClient.serviceName = "unknown";
	connections.kbChainedEntriesEventClient.interval = 1;
	connections.kbChainedEntriesEventClient.roboticMiddleware = "ACE_SmartSoft";
	connections.dummy.minActFreq = 0.0;
	connections.dummy.maxActFreq = 0.0;
	// scheduling default parameters
	connections.dummy.scheduler = "DEFAULT";
	connections.dummy.priority = -1;
	connections.dummy.cpuAffinity = -1;
	connections.kbChainedEntriesEventClientHandler.prescale = 1;
	
	// initialize members of ComponentKBROSExtension
	
	// initialize members of PlainOpcUaComponentKBExtension
	
}

void ComponentKB::addPortFactory(const std::string &name, ComponentKBPortFactoryInterface *portFactory)
{
	portFactoryRegistry[name] = portFactory;
}

void ComponentKB::addExtension(ComponentKBExtension *extension)
{
	componentExtensionRegistry[extension->getName()] = extension;
}

SmartACE::SmartComponent* ComponentKB::getComponentImpl()
{
	return dynamic_cast<ComponentKBAcePortFactory*>(portFactoryRegistry["ACE_SmartSoft"])->getComponentImpl();
}

/**
 * Notify the component that setup/initialization is finished.
 * You may call this function from anywhere in the component.
 *
 * Set component's internal lifecycle state automaton (if any) into 
 * Alive mode (from here on the component is ready to provide its services)
 */
void ComponentKB::setStartupFinished() {
	stateSlave->setWaitState("Alive");
	std::cout << "ComponentDefinition initialization/startup finished." << std::endl;
}


Smart::StatusCode ComponentKB::connectKbChainedEntriesEventClient(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	if(connections.kbChainedEntriesEventClient.initialConnect == false) {
		return Smart::SMART_OK;
	}
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = kbChainedEntriesEventClient->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->kbChainedEntriesEventClient->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	return status;
}


/**
 * First connect ALL client ports contained in this component, then start all services:
 * activate state, push, etc...
 */
Smart::StatusCode ComponentKB::connectAndStartAllServices() {
	Smart::StatusCode status = Smart::SMART_OK;
	
	status = connectKbChainedEntriesEventClient(connections.kbChainedEntriesEventClient.serverName, connections.kbChainedEntriesEventClient.serviceName);
	if(status != Smart::SMART_OK) return status;
	return status;
}

/**
 * Start all tasks contained in this component.
 */
void ComponentKB::startAllTasks() {
	// start task dummy
	if(connections.dummy.scheduler != "DEFAULT") {
		ACE_Sched_Params dummy_SchedParams(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(connections.dummy.scheduler == "FIFO") {
			dummy_SchedParams.policy(ACE_SCHED_FIFO);
			dummy_SchedParams.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(connections.dummy.scheduler == "RR") {
			dummy_SchedParams.policy(ACE_SCHED_RR);
			dummy_SchedParams.priority(ACE_THR_PRI_RR_MIN);
		}
		dummy->start(dummy_SchedParams, connections.dummy.cpuAffinity);
	} else {
		dummy->start();
	}
}

/**
 * Start all timers contained in this component
 */
void ComponentKB::startAllTimers() {
}


Smart::TaskTriggerSubject* ComponentKB::getInputTaskTriggerFromString(const std::string &client)
{
	if(client == "kbChainedEntriesEventClient") return kbChainedEntriesEventClientInputTaskTrigger;
	
	return NULL;
}


void ComponentKB::init(int argc, char *argv[])
{
	try {
		Smart::StatusCode status;
		
		// load initial parameters from ini-file (if found)
		loadParameter(argc, argv);
		
		// print out the actual parameters which are used to initialize the component
		std::cout << " \nComponentDefinition Initial-Parameters:\n" << COMP->getParameters() << std::endl;
		
		// initializations of ComponentKBROSExtension
		
		// initializations of PlainOpcUaComponentKBExtension
		
		
		// initialize all registered port-factories
		for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
		{
			portFactory->second->initialize(this, argc, argv);
		}
		
		// initialize all registered component-extensions
		for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
		{
			extension->second->initialize(this, argc, argv);
		}
		
		ComponentKBPortFactoryInterface *acePortFactory = portFactoryRegistry["ACE_SmartSoft"];
		if(acePortFactory == 0) {
			std::cerr << "ERROR: acePortFactory NOT instantiated -> exit(-1)" << std::endl;
			exit(-1);
		}
		
		// this pointer is used for backwards compatibility (deprecated: should be removed as soon as all patterns, including coordination, are moved to port-factory)
		SmartACE::SmartComponent *component = dynamic_cast<ComponentKBAcePortFactory*>(acePortFactory)->getComponentImpl();
		
		std::cout << "ComponentDefinition ComponentKB is named " << connections.component.name << std::endl;
		
		if(connections.component.useLogger == true) {
			//FIXME: use logging
			//Smart::LOGGER->openLogFileInFolder("data/"+connections.component.name);
			//Smart::LOGGER->startLogging();
		}

		// create event-test handlers (if needed)
		kbEventServerEventTestHandler = new KbEventServerEventTestHandler();
		
		// create server ports
		// TODO: set minCycleTime from Ini-file
		kbEventServer = portFactoryRegistry[connections.kbEventServer.roboticMiddleware]->createKbEventServer(connections.kbEventServer.serviceName, kbEventServerEventTestHandler);
		kbQuery = portFactoryRegistry[connections.kbQuery.roboticMiddleware]->createKbQuery(connections.kbQuery.serviceName);
		kbQueryInputTaskTrigger = new Smart::QueryServerTaskTrigger<CommBasicObjects::CommKBRequest, CommBasicObjects::CommKBResponse,SmartACE::QueryId>(kbQuery);
		
		// create client ports
		kbChainedEntriesEventClient = portFactoryRegistry[connections.kbChainedEntriesEventClient.roboticMiddleware]->createKbChainedEntriesEventClient();
		
		// create InputTaskTriggers and UpcallManagers
		kbChainedEntriesEventClientInputTaskTrigger = new Smart::InputTaskTrigger<Smart::EventInputType<CommBasicObjects::CommKBEventResult,SmartACE::EventId>>(kbChainedEntriesEventClient);
		kbChainedEntriesEventClientUpcallManager = new KbChainedEntriesEventClientUpcallManager(kbChainedEntriesEventClient);
		
		// create input-handler
		kbChainedEntriesEventClientHandler = new KbChainedEntriesEventClientHandler(kbChainedEntriesEventClient, connections.kbChainedEntriesEventClientHandler.prescale);
		
		// create request-handlers
		kbQueryHandler = new KbQueryHandler(kbQuery);
		
		// create state pattern
		stateChangeHandler = new SmartStateChangeHandler();
		stateSlave = new SmartACE::StateSlave(component, stateChangeHandler);
		status = stateSlave->setUpInitialState(connections.component.initialComponentMode);
		if (status != Smart::SMART_OK) std::cerr << status << "; failed setting initial ComponentMode: " << connections.component.initialComponentMode << std::endl;
		// activate state slave
		status = stateSlave->activate();
		if(status != Smart::SMART_OK) std::cerr << "ERROR: activate state" << std::endl;
		
		wiringSlave = new SmartACE::WiringSlave(component);
		// add client port to wiring slave
		if(connections.kbChainedEntriesEventClient.roboticMiddleware == "ACE_SmartSoft") {
			//FIXME: this must also work with other implementations
			dynamic_cast<SmartACE::EventClient<CommBasicObjects::CommKBEventParam, CommBasicObjects::CommKBEventResult>*>(kbChainedEntriesEventClient)->add(wiringSlave, connections.kbChainedEntriesEventClient.wiringName);
		}
		
		// create parameter slave
		param = new SmartACE::ParameterSlave(component, &paramHandler);
		
		
		// create Task dummy
		dummy = new Dummy(component);
		// configure input-links
		// configure task-trigger (if task is configurable)
		if(connections.dummy.trigger == "PeriodicTimer") {
			// create PeriodicTimerTrigger
			int microseconds = 1000*1000 / connections.dummy.periodicActFreq;
			if(microseconds > 0) {
				Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
				triggerPtr->attach(dummy);
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				// store trigger in class member
				dummyTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task Dummy" << std::endl;
			}
		} else if(connections.dummy.trigger == "DataTriggered") {
			dummyTrigger = getInputTaskTriggerFromString(connections.dummy.inPortRef);
			if(dummyTrigger != NULL) {
				dummyTrigger->attach(dummy, connections.dummy.prescale);
			} else {
				std::cerr << "ERROR: could not set-up InPort " << connections.dummy.inPortRef << " as activation source for Task Dummy" << std::endl;
			}
		} 
		
		
		// link observers with subjects
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std exception" << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}

// run the component
void ComponentKB::run()
{
	stateSlave->acquire("init");
	// startup all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->onStartup();
	}
	
	// startup all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->onStartup();
	}
	stateSlave->release("init");
	
	// do not call this handler within the init state (see above) as this handler internally calls setStartupFinished() (this should be fixed in future)
	compHandler.onStartup();
	
	// this call blocks until the component is commanded to shutdown
	stateSlave->acquire("shutdown");
	
	// shutdown all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->onShutdown();
	}
	
	// shutdown all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->onShutdown();
	}
	
	if(connections.component.useLogger == true) {
		//FIXME: use logging
		//Smart::LOGGER->stopLogging();
	}
	
	compHandler.onShutdown();
	
	stateSlave->release("shutdown");
}

// clean-up component's resources
void ComponentKB::fini()
{
	// unlink all observers
	
	// destroy all task instances
	// unlink all UpcallManagers
	// unlink the TaskTrigger
	if(dummyTrigger != NULL){
		dummyTrigger->detach(dummy);
		delete dummy;
	}

	// destroy all input-handler
	delete kbChainedEntriesEventClientHandler;

	// destroy InputTaskTriggers and UpcallManagers
	delete kbChainedEntriesEventClientInputTaskTrigger;
	delete kbChainedEntriesEventClientUpcallManager;

	// destroy client ports
	delete kbChainedEntriesEventClient;

	// destroy server ports
	delete kbEventServer;
	delete kbQuery;
	delete kbQueryInputTaskTrigger;
	// destroy event-test handlers (if needed)
	delete kbEventServerEventTestHandler;
	
	// destroy request-handlers
	delete kbQueryHandler;
	
	delete stateSlave;
	// destroy state-change-handler
	delete stateChangeHandler;
	
	// destroy all master/slave ports
	delete wiringSlave;
	delete param;
	

	// destroy all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->destroy();
	}

	// destroy all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->destroy();
	}
	
	// destruction of ComponentKBROSExtension
	
	// destruction of PlainOpcUaComponentKBExtension
	
}

void ComponentKB::loadParameter(int argc, char *argv[])
{
	/*
	 Parameters can be specified via command line --filename=<filename> or -f <filename>

	 With this parameter present:
	   - The component will look for the file in the current working directory,
	     a path relative to the current directory or any absolute path
	   - The component will use the default values if the file cannot be found

	 With this parameter absent:
	   - <Name of Component>.ini will be read from current working directory, if found there
	   - $SMART_ROOT/etc/<Name of Component>.ini will be read otherwise
	   - Default values will be used if neither found in working directory or /etc
	 */
	SmartACE::SmartIniParameter parameter;
	std::ifstream parameterfile;
	bool parameterFileFound = false;

	// load parameters
	try
	{
		// if paramfile is given as argument
		if(parameter.tryAddFileFromArgs(argc,argv,"filename", 'f'))
		{
			parameterFileFound = true;
			std::cout << "parameter file is loaded from an argv argument \n";
		} else if(parameter.searchFile("ComponentKB.ini", parameterfile)) {
			parameterFileFound = true;
			std::cout << "load ComponentKB.ini parameter file\n";
			parameter.addFile(parameterfile);
		} else {
			std::cout << "WARNING: ComponentKB.ini parameter file not found! (using default values or command line arguments)\n";
		}
		
		// add command line arguments to allow overwriting of parameters
		// from file
		parameter.addCommandLineArgs(argc,argv,"component");
		
		// initialize the naming service using the command line parameters parsed in the
		// SmartIniParameter class. The naming service parameters are expected to be in
		// the "component" parameter group.
		SmartACE::NAMING::instance()->checkForHelpArg(argc,argv);
		if(parameterFileFound) 
		{
			if(SmartACE::NAMING::instance()->init(parameter.getAllParametersFromGroup("component")) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		} else {
			if(SmartACE::NAMING::instance()->init(argc, argv) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		}
			
		// print all known parameters
		// parameter.print();
		
		//--- server port // client port // other parameter ---
		// load parameter
		parameter.getString("component", "name", connections.component.name);
		parameter.getString("component", "initialComponentMode", connections.component.initialComponentMode);
		if(parameter.checkIfParameterExists("component", "defaultScheduler")) {
			parameter.getString("component", "defaultScheduler", connections.component.defaultScheduler);
		}
		if(parameter.checkIfParameterExists("component", "useLogger")) {
			parameter.getBoolean("component", "useLogger", connections.component.useLogger);
		}
		
		// load parameters for client kbChainedEntriesEventClient
		parameter.getBoolean("kbChainedEntriesEventClient", "initialConnect", connections.kbChainedEntriesEventClient.initialConnect);
		parameter.getString("kbChainedEntriesEventClient", "serviceName", connections.kbChainedEntriesEventClient.serviceName);
		parameter.getString("kbChainedEntriesEventClient", "serverName", connections.kbChainedEntriesEventClient.serverName);
		parameter.getString("kbChainedEntriesEventClient", "wiringName", connections.kbChainedEntriesEventClient.wiringName);
		if(parameter.checkIfParameterExists("kbChainedEntriesEventClient", "roboticMiddleware")) {
			parameter.getString("kbChainedEntriesEventClient", "roboticMiddleware", connections.kbChainedEntriesEventClient.roboticMiddleware);
		}
		
		// load parameters for server kbEventServer
		parameter.getString("kbEventServer", "serviceName", connections.kbEventServer.serviceName);
		if(parameter.checkIfParameterExists("kbEventServer", "roboticMiddleware")) {
			parameter.getString("kbEventServer", "roboticMiddleware", connections.kbEventServer.roboticMiddleware);
		}
		// load parameters for server kbQuery
		parameter.getString("kbQuery", "serviceName", connections.kbQuery.serviceName);
		if(parameter.checkIfParameterExists("kbQuery", "roboticMiddleware")) {
			parameter.getString("kbQuery", "roboticMiddleware", connections.kbQuery.roboticMiddleware);
		}
		
		// load parameters for task dummy
		parameter.getDouble("dummy", "minActFreqHz", connections.dummy.minActFreq);
		parameter.getDouble("dummy", "maxActFreqHz", connections.dummy.maxActFreq);
		parameter.getString("dummy", "triggerType", connections.dummy.trigger);
		if(connections.dummy.trigger == "PeriodicTimer") {
			parameter.getDouble("dummy", "periodicActFreqHz", connections.dummy.periodicActFreq);
		} else if(connections.dummy.trigger == "DataTriggered") {
			parameter.getString("dummy", "inPortRef", connections.dummy.inPortRef);
			parameter.getInteger("dummy", "prescale", connections.dummy.prescale);
		}
		if(parameter.checkIfParameterExists("dummy", "scheduler")) {
			parameter.getString("dummy", "scheduler", connections.dummy.scheduler);
		}
		if(parameter.checkIfParameterExists("dummy", "priority")) {
			parameter.getInteger("dummy", "priority", connections.dummy.priority);
		}
		if(parameter.checkIfParameterExists("dummy", "cpuAffinity")) {
			parameter.getInteger("dummy", "cpuAffinity", connections.dummy.cpuAffinity);
		}
		if(parameter.checkIfParameterExists("kbChainedEntriesEventClientHandler", "prescale")) {
			parameter.getInteger("kbChainedEntriesEventClientHandler", "prescale", connections.kbChainedEntriesEventClientHandler.prescale);
		}
		
		// load parameters for ComponentKBROSExtension
		
		// load parameters for PlainOpcUaComponentKBExtension
		
		
		// load parameters for all registered component-extensions
		for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
		{
			extension->second->loadParameters(parameter);
		}
		
		paramHandler.loadParameter(parameter);
	
	} catch (const SmartACE::IniParameterError & e) {
		std::cerr << e.what() << std::endl;
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std::exception: " << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}
