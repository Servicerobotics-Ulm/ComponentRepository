//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _COMPONENTROBOTINOIRSERVER_HH
#define _COMPONENTROBOTINOIRSERVER_HH

#include <map>
#include <iostream>
#include "aceSmartSoft.hh"
#include "smartQueryServerTaskTrigger_T.h"
#include "ComponentRobotinoIRServerCore.hh"

#include "ComponentRobotinoIRServerPortFactoryInterface.hh"
#include "ComponentRobotinoIRServerExtension.hh"

// forward declarations
class ComponentRobotinoIRServerPortFactoryInterface;
class ComponentRobotinoIRServerExtension;

// includes for ComponentRobotinoIRServerROS1InterfacesExtension

// includes for ComponentRobotinoIRServerRestInterfacesExtension

// includes for PlainOpcUaComponentRobotinoIRServerExtension
// include plain OPC UA device clients
// include plain OPC UA status servers


// include communication objects
#include <CommBasicObjects/CommBaseState.hh>
#include <CommBasicObjects/CommBaseStateACE.hh>
#include <CommBasicObjects/CommMobileIRScan.hh>
#include <CommBasicObjects/CommMobileIRScanACE.hh>

// include tasks
#include "IRTask.hh"
// include UpcallManagers and InputCollectors
#include "BaseStateServiceInUpcallManager.hh"
#include "BaseStateServiceInInputCollector.hh"

// include input-handler(s)
// include request-handler(s)
// output port wrappers
#include "MobileIRPushOutWrapper.hh"
#include "AdditionalIRScanPushOutWrapper.hh"

// include handler
#include "CompHandler.hh"

#include "ParameterStateStruct.hh"
#include "ParameterUpdateHandler.hh"

#include "SmartStateChangeHandler.hh"

#define COMP ComponentRobotinoIRServer::instance()

class ComponentRobotinoIRServer : public ComponentRobotinoIRServerCore {
private:
	static ComponentRobotinoIRServer *_componentRobotinoIRServer;
	
	// constructor
	ComponentRobotinoIRServer();
	
	// copy-constructor
	ComponentRobotinoIRServer(const ComponentRobotinoIRServer& cc);
	
	// destructor
	~ComponentRobotinoIRServer() { };
	
	// load parameter from ini file
	void loadParameter(int argc, char* argv[]);
	
	// instantiate comp-handler
	CompHandler compHandler;
	
	// helper method that maps a string-name to an according TaskTriggerSubject
	Smart::TaskTriggerSubject* getInputTaskTriggerFromString(const std::string &client);
	
	// internal map storing the different port-creation factories (that internally map to specific middleware implementations)
	std::map<std::string, ComponentRobotinoIRServerPortFactoryInterface*> portFactoryRegistry;
	
	// internal map storing various extensions of this component class
	std::map<std::string, ComponentRobotinoIRServerExtension*> componentExtensionRegistry;
	
public:
	ParameterStateStruct getGlobalState() const
	{
		return paramHandler.getGlobalState();
	}
	
	ParameterStateStruct getParameters() const
	{
		return paramHandler.getGlobalState();
	}
	
	// define tasks
	Smart::TaskTriggerSubject* iRTaskTrigger;
	IRTask *iRTask;
	
	// define input-ports
	// InputPort BaseStateServiceIn
	Smart::IPushClientPattern<CommBasicObjects::CommBaseState> *baseStateServiceIn;
	Smart::InputTaskTrigger<CommBasicObjects::CommBaseState> *baseStateServiceInInputTaskTrigger;
	BaseStateServiceInUpcallManager *baseStateServiceInUpcallManager;
	BaseStateServiceInInputCollector *baseStateServiceInInputCollector;
	
	// define request-ports
	
	// define input-handler
	
	// define output-ports
	Smart::IPushServerPattern<CommBasicObjects::CommMobileIRScan> *additionalIRScanPushOut;
	AdditionalIRScanPushOutWrapper *additionalIRScanPushOutWrapper;
	Smart::IPushServerPattern<CommBasicObjects::CommMobileIRScan> *mobileIRPushOut;
	MobileIRPushOutWrapper *mobileIRPushOutWrapper;
	
	// define answer-ports
	
	// define request-handlers
	
	// definitions of ComponentRobotinoIRServerROS1InterfacesExtension
	
	// definitions of ComponentRobotinoIRServerRestInterfacesExtension
	
	// definitions of PlainOpcUaComponentRobotinoIRServerExtension
	
	
	// define default slave ports
	SmartACE::StateSlave *stateSlave;
	SmartStateChangeHandler *stateChangeHandler;
	SmartACE::WiringSlave *wiringSlave;
	ParamUpdateHandler paramHandler;
	SmartACE::ParameterSlave *param;
	
	
	/// this method is used to register different PortFactory classes (one for each supported middleware framework)
	void addPortFactory(const std::string &name, ComponentRobotinoIRServerPortFactoryInterface *portFactory);
	
	SmartACE::SmartComponent* getComponentImpl();
	
	/// this method is used to register different component-extension classes
	void addExtension(ComponentRobotinoIRServerExtension *extension);
	
	/// this method allows to access the registered component-extensions (automatically converting to the actuall implementation type)
	template <typename T>
	T* getExtension(const std::string &name) {
		auto it = componentExtensionRegistry.find(name);
		if(it != componentExtensionRegistry.end()) {
			return dynamic_cast<T*>(it->second);
		}
		return 0;
	}
	
	/// initialize component's internal members
	void init(int argc, char *argv[]);
	
	/// execute the component's infrastructure
	void run();
	
	/// clean-up component's resources
	void fini();
	
	/// call this method to set the overall component into the Alive state (i.e. component is then ready to operate)
	void setStartupFinished();
	
	/// connect all component's client ports
	Smart::StatusCode connectAndStartAllServices();
	
	/// start all assocuated Activities
	void startAllTasks();
	
	/// start all associated timers
	void startAllTimers();
	
	Smart::StatusCode connectBaseStateServiceIn(const std::string &serverName, const std::string &serviceName);

	// return singleton instance
	static ComponentRobotinoIRServer* instance()
	{
		if(_componentRobotinoIRServer == 0) {
			_componentRobotinoIRServer = new ComponentRobotinoIRServer();
		}
		return _componentRobotinoIRServer;
	}
	
	static void deleteInstance() {
		if(_componentRobotinoIRServer != 0) {
			delete _componentRobotinoIRServer;
		}
	}
	
	// connections parameter
	struct connections_struct
	{
		// component struct
		struct component_struct
		{
			// the name of the component
			std::string name;
			std::string initialComponentMode;
			std::string defaultScheduler;
			bool useLogger;
		} component;
		
		//--- task parameter ---
		struct IRTask_struct {
			double minActFreq;
			double maxActFreq;
			std::string trigger;
			// only one of the following two params is 
			// actually used at run-time according 
			// to the system config model
			double periodicActFreq;
			// or
			std::string inPortRef;
			int prescale;
			// scheduling parameters
			std::string scheduler;
			int priority;
			int cpuAffinity;
		} iRTask;
		
		//--- upcall parameter ---
		
		//--- server port parameter ---
		struct AdditionalIRScanPushOut_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} additionalIRScanPushOut;
		struct MobileIRPushOut_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} mobileIRPushOut;
	
		//--- client port parameter ---
		struct BaseStateServiceIn_struct {
			bool initialConnect;
			std::string serverName;
			std::string serviceName;
			std::string wiringName;
			long interval;
			std::string roboticMiddleware;
		} baseStateServiceIn;
		
		// -- parameters for ComponentRobotinoIRServerROS1InterfacesExtension
		
		// -- parameters for ComponentRobotinoIRServerRestInterfacesExtension
		
		// -- parameters for PlainOpcUaComponentRobotinoIRServerExtension
		
	} connections;
};
#endif
