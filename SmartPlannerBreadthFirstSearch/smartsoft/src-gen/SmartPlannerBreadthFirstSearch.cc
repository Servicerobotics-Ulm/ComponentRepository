//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "SmartPlannerBreadthFirstSearch.hh"
#include "smartTimedTaskTrigger.h"
//FIXME: implement logging
//#include "smartGlobalLogger.hh"

#include "PlannerEventServerEventTestHandler.hh"

// constructor
SmartPlannerBreadthFirstSearch::SmartPlannerBreadthFirstSearch()
{
	std::cout << "constructor of SmartPlannerBreadthFirstSearch\n";
	
	component = NULL;
	
	// set all pointer members to NULL
	baseStateClient = NULL;
	baseStateClientInputTaskTrigger = NULL;
	baseStateClientUpcallManager = NULL;
	curMapClient = NULL;
	curMapClientInputTaskTrigger = NULL;
	curMapClientUpcallManager = NULL;
	plannerEventServer = NULL;
	plannerEventServerEventTestHandler = NULL; 
	plannerGoalServer = NULL;
	plannerTask = NULL;
	plannerTaskTrigger = NULL;
	stateChangeHandler = NULL;
	stateSlave = NULL;
	wiringSlave = NULL;
	param = NULL;
	
	// set default ini parameter values
	connections.component.name = "SmartPlannerBreadthFirstSearch";
	connections.component.initialMainState = "Active";
	connections.component.defaultScheduler = "DEFAULT";
	connections.component.useLogger = false;
	
	connections.plannerEventServer.serviceName = "PlannerEventServer";
	connections.plannerGoalServer.serviceName = "PlannerGoalServer";
	connections.baseStateClient.serverName = "unknown";
	connections.baseStateClient.serviceName = "unknown";
	connections.baseStateClient.interval = 1;
	connections.curMapClient.serverName = "unknown";
	connections.curMapClient.serviceName = "unknown";
	connections.curMapClient.interval = 1;
	connections.plannerTask.minActFreq = 4.0;
	connections.plannerTask.maxActFreq = 10.0;
	connections.plannerTask.prescale = 1;
	connections.plannerTask.trigger = "PeriodicTimer";
	connections.plannerTask.periodicActFreq = 4.0;
	// scheduling default parameters
	connections.plannerTask.scheduler = "DEFAULT";
	connections.plannerTask.priority = -1;
	connections.plannerTask.cpuAffinity = -1;
}



/**
 * Notify the component that setup/initialization is finished.
 * You may call this function from anywhere in the component.
 *
 * Set component's internal lifecycle state automaton (if any) into 
 * Alive mode (from here on the component is ready to provide its services)
 */
void SmartPlannerBreadthFirstSearch::setStartupFinished() {
	stateSlave->setWaitState("Alive");
	std::cout << "ComponentDefinition initialization/startup finished." << std::endl;
}


Smart::StatusCode SmartPlannerBreadthFirstSearch::connectBaseStateClient(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = baseStateClient->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->baseStateClient->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	baseStateClient->subscribe(connections.baseStateClient.interval);
	return status;
}
Smart::StatusCode SmartPlannerBreadthFirstSearch::connectCurMapClient(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = curMapClient->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->curMapClient->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	curMapClient->subscribe(connections.curMapClient.interval);
	return status;
}


/**
 * First connect ALL client ports contained in this component, then start all services:
 * activate state, push, etc...
 */
Smart::StatusCode SmartPlannerBreadthFirstSearch::connectAndStartAllServices() {
	Smart::StatusCode status = Smart::SMART_OK;
	
	status = connectBaseStateClient(connections.baseStateClient.serverName, connections.baseStateClient.serviceName);
	if(status != Smart::SMART_OK) return status;
	status = connectCurMapClient(connections.curMapClient.serverName, connections.curMapClient.serviceName);
	if(status != Smart::SMART_OK) return status;
	
	return status;
}

/**
 * Start all tasks contained in this component.
 */
void SmartPlannerBreadthFirstSearch::startAllTasks() {
	// start task PlannerTask
	if(connections.plannerTask.scheduler != "DEFAULT") {
		ACE_Sched_Params plannerTask_SchedParams(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(connections.plannerTask.scheduler == "FIFO") {
			plannerTask_SchedParams.policy(ACE_SCHED_FIFO);
			plannerTask_SchedParams.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(connections.plannerTask.scheduler == "RR") {
			plannerTask_SchedParams.policy(ACE_SCHED_RR);
			plannerTask_SchedParams.priority(ACE_THR_PRI_RR_MIN);
		}
		plannerTask->start(plannerTask_SchedParams, connections.plannerTask.cpuAffinity);
	} else {
		plannerTask->start();
	}
}

/**
 * Start all timers contained in this component
 */
void SmartPlannerBreadthFirstSearch::startAllTimers() {
}


Smart::TaskTriggerSubject* SmartPlannerBreadthFirstSearch::getInputTaskTriggerFromString(const std::string &client)
{
	if(client == "BaseStateClient") return baseStateClientInputTaskTrigger;
	if(client == "CurMapClient") return curMapClientInputTaskTrigger;
	
	return NULL;
}


void SmartPlannerBreadthFirstSearch::init(int argc, char *argv[])
{
	try {
		Smart::StatusCode status;
		
		// load initial parameters from ini-file (if found)
		loadParameter(argc, argv);
		
		// print out the actual parameters which are used to initialize the component
		std::cout << " \nComponentDefinition Initial-Parameters:\n" << COMP->getGlobalState() << std::endl;
		if(connections.component.defaultScheduler != "DEFAULT") {
			ACE_Sched_Params sched_params(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
			if(connections.component.defaultScheduler == "FIFO") {
				sched_params.policy(ACE_SCHED_FIFO);
				sched_params.priority(ACE_THR_PRI_FIFO_MIN);
			} else if(connections.component.defaultScheduler == "RR") {
				sched_params.policy(ACE_SCHED_RR);
				sched_params.priority(ACE_THR_PRI_RR_MIN);
			}
			// create new instance of the SmartSoft component with customized scheuling parameters 
			component = new SmartPlannerBreadthFirstSearchImpl(connections.component.name, argc, argv, sched_params);
		} else {
			// create new instance of the SmartSoft component
			component = new SmartPlannerBreadthFirstSearchImpl(connections.component.name, argc, argv);
		}
		
		
		std::cout << "ComponentDefinition SmartPlannerBreadthFirstSearch is named " << connections.component.name << std::endl;
		
		if(connections.component.useLogger == true) {
			//FIXME: use logging
			//Smart::LOGGER->openLogFileInFolder("data/"+connections.component.name);
			//Smart::LOGGER->startLogging();
		}

		// create event-test handlers (if needed)
		plannerEventServerEventTestHandler = new PlannerEventServerEventTestHandler();
		
		// create server ports
		// TODO: set minCycleTime from Ini-file
		plannerEventServer = new SmartACE::EventServer<CommNavigationObjects::CommPlannerEventParameter, CommNavigationObjects::CommPlannerEventResult, CommNavigationObjects::PlannerEventState>(component, connections.plannerEventServer.serviceName, plannerEventServerEventTestHandler);
		plannerGoalServer = new SmartACE::PushServer<CommNavigationObjects::CommPlannerGoal>(component, connections.plannerGoalServer.serviceName);
		
		// create client ports
		baseStateClient = new SmartACE::PushClient<CommBasicObjects::CommBaseState>(component);
		curMapClient = new SmartACE::PushClient<CommNavigationObjects::CommGridMap>(component);
		
		// create InputTaskTriggers and UpcallManagers
		baseStateClientInputTaskTrigger = new Smart::InputTaskTrigger<CommBasicObjects::CommBaseState>(baseStateClient);
		baseStateClientUpcallManager = new BaseStateClientUpcallManager(baseStateClient);
		curMapClientInputTaskTrigger = new Smart::InputTaskTrigger<CommNavigationObjects::CommGridMap>(curMapClient);
		curMapClientUpcallManager = new CurMapClientUpcallManager(curMapClient);
		
		// create input-handler
		
		// create request-handlers
		
		// create state pattern
		stateChangeHandler = new SmartStateChangeHandler();
		stateSlave = new SmartACE::StateSlave(component, stateChangeHandler);
		// create one default main state called Active which should be entered by default
		if (stateSlave->defineStates("Active" ,"active") != Smart::SMART_OK) std::cerr << "ERROR: define state" << std::endl;
		if (stateSlave->setUpInitialState(connections.component.initialMainState) != Smart::SMART_OK) std::cerr << "ERROR: setUpInitialState" << std::endl;
		// activate state slave
		status = stateSlave->activate();
		if(status != Smart::SMART_OK) std::cerr << "ERROR: activate state" << std::endl;
		
		wiringSlave = new SmartACE::WiringSlave(component);
		// add client port to wiring slave
		dynamic_cast<SmartACE::PushClient<CommBasicObjects::CommBaseState>*>(baseStateClient)->add(wiringSlave, connections.baseStateClient.wiringName);
		dynamic_cast<SmartACE::PushClient<CommNavigationObjects::CommGridMap>*>(curMapClient)->add(wiringSlave, connections.curMapClient.wiringName);
		
		// create parameter slave
		param = new SmartACE::ParameterSlave(component, &paramHandler);
		
		
		// create Task PlannerTask
		plannerTask = new PlannerTask(component);
		// configure input-links
		baseStateClientUpcallManager->attach(plannerTask);
		curMapClientUpcallManager->attach(plannerTask);
		// configure task-trigger (if task is configurable)
		if(connections.plannerTask.trigger == "PeriodicTimer") {
			// create PeriodicTimerTrigger
			int microseconds = 1000*1000 / connections.plannerTask.periodicActFreq;
			if(microseconds > 0) {
				Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
				triggerPtr->attach(plannerTask);
				component->getTimerManager()->scheduleTimer(triggerPtr, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				// store trigger in class member
				plannerTaskTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task PlannerTask" << std::endl;
			}
		} else if(connections.plannerTask.trigger == "DataTriggered") {
			plannerTaskTrigger = getInputTaskTriggerFromString(connections.plannerTask.inPortRef);
			if(plannerTaskTrigger != NULL) {
				plannerTaskTrigger->attach(plannerTask, connections.plannerTask.prescale);
			} else {
				std::cerr << "ERROR: could not set-up InPort " << connections.plannerTask.inPortRef << " as activation source for Task PlannerTask" << std::endl;
			}
		} else
		{
			// setup default task-trigger as PeriodicTimer
			Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
			int microseconds = 1000*1000 / 4.0;
			if(microseconds > 0) {
				component->getTimerManager()->scheduleTimer(triggerPtr, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				triggerPtr->attach(plannerTask);
				// store trigger in class member
				plannerTaskTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task PlannerTask" << std::endl;
			}
		}
		
		
		// link observers with subjects
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std exception" << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}

// run the component
void SmartPlannerBreadthFirstSearch::run()
{
	compHandler.onStartup();
	
	// coponent will now start running and will continue (block in the run method) until it is commanded to shutdown (i.e. by a SIGINT signal)
	component->run();
	// component was signalled to shutdown
	// 1) signall all tasks to shutdown as well (and give them 2 seconds time to cooperate)
	// if time exceeds, component is killed without further clean-up
	component->closeAllAssociatedTasks(2);
	
	if(connections.component.useLogger == true) {
		//FIXME: use logging
		//Smart::LOGGER->stopLogging();
	}
	
	compHandler.onShutdown();
	
	// unlink all observers
	
	// destroy all task instances
	// unlink all UpcallManagers
	baseStateClientUpcallManager->detach(plannerTask);
	curMapClientUpcallManager->detach(plannerTask);
	// unlink the TaskTrigger
	plannerTaskTrigger->detach(plannerTask);
	delete plannerTask;

	// destroy all input-handler

	// destroy InputTaskTriggers and UpcallManagers
	delete baseStateClientInputTaskTrigger;
	delete baseStateClientUpcallManager;
	delete curMapClientInputTaskTrigger;
	delete curMapClientUpcallManager;

	// destroy client ports
	delete baseStateClient;
	delete curMapClient;

	// destroy server ports
	delete plannerEventServer;
	delete plannerGoalServer;
	// destroy event-test handlers (if needed)
	delete plannerEventServerEventTestHandler;
	
	// create request-handlers

	delete stateSlave;
	// delete state-change-handler
	delete stateChangeHandler;
	
	// delete all master/slave ports
	delete wiringSlave;
	delete param;
	

	// clean-up component's internally used resources (internally used communication middleware) 
	component->cleanUpComponentResources();
	
	// finally delete the component itself
	delete component;
}

void SmartPlannerBreadthFirstSearch::loadParameter(int argc, char *argv[])
{
	/*
	 Parameters can be specified via command line --filename=<filename> or -f <filename>

	 With this parameter present:
	   - The component will look for the file in the current working directory,
	     a path relative to the current directory or any absolute path
	   - The component will use the default values if the file cannot be found

	 With this parameter absent:
	   - <Name of Component>.ini will be read from current working directory, if found there
	   - $SMART_ROOT/etc/<Name of Component>.ini will be read otherwise
	   - Default values will be used if neither found in working directory or /etc
	 */
	SmartACE::SmartIniParameter parameter;
	std::ifstream parameterfile;
	bool parameterFileFound = false;

	// load parameters
	try
	{
		// if paramfile is given as argument
		if(parameter.tryAddFileFromArgs(argc,argv,"filename", 'f'))
		{
			parameterFileFound = true;
			std::cout << "parameter file is loaded from an argv argument \n";
		} else if(parameter.searchFile("SmartPlannerBreadthFirstSearch.ini", parameterfile)) {
			parameterFileFound = true;
			std::cout << "load SmartPlannerBreadthFirstSearch.ini parameter file\n";
			parameter.addFile(parameterfile);
		} else {
			std::cout << "WARNING: SmartPlannerBreadthFirstSearch.ini parameter file not found! (using default values or command line arguments)\n";
		}
		
		// add command line arguments to allow overwriting of parameters
		// from file
		parameter.addCommandLineArgs(argc,argv,"component");
		
		// initialize the naming service using the command line parameters parsed in the
		// SmartIniParameter class. The naming service parameters are expected to be in
		// the "component" parameter group.
		SmartACE::NAMING::instance()->checkForHelpArg(argc,argv);
		if(parameterFileFound) 
		{
			if(SmartACE::NAMING::instance()->init(parameter.getAllParametersFromGroup("component")) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		} else {
			if(SmartACE::NAMING::instance()->init(argc, argv) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		}
			
		// print all known parameters
		// parameter.print();
		
		//--- server port // client port // other parameter ---
		// load parameter
		parameter.getString("component", "name", connections.component.name);
		parameter.getString("component", "initialMainState", connections.component.initialMainState);
		if(parameter.checkIfParameterExists("component", "defaultScheduler")) {
			parameter.getString("component", "defaultScheduler", connections.component.defaultScheduler);
		}
		if(parameter.checkIfParameterExists("component", "useLogger")) {
			parameter.getBoolean("component", "useLogger", connections.component.useLogger);
		}
		
		// load parameters for client BaseStateClient
		parameter.getString("baseStateClient", "serviceName", connections.baseStateClient.serviceName);
		parameter.getString("baseStateClient", "serverName", connections.baseStateClient.serverName);
		parameter.getString("baseStateClient", "wiringName", connections.baseStateClient.wiringName);
		parameter.getInteger("baseStateClient", "interval", connections.baseStateClient.interval);
		// load parameters for client CurMapClient
		parameter.getString("curMapClient", "serviceName", connections.curMapClient.serviceName);
		parameter.getString("curMapClient", "serverName", connections.curMapClient.serverName);
		parameter.getString("curMapClient", "wiringName", connections.curMapClient.wiringName);
		parameter.getInteger("curMapClient", "interval", connections.curMapClient.interval);
		
		// load parameters for server PlannerEventServer
		parameter.getString("plannerEventServer", "serviceName", connections.plannerEventServer.serviceName);
		// load parameters for server PlannerGoalServer
		parameter.getString("plannerGoalServer", "serviceName", connections.plannerGoalServer.serviceName);
		
		// load parameters for task PlannerTask
		parameter.getDouble("PlannerTask", "minActFreqHz", connections.plannerTask.minActFreq);
		parameter.getDouble("PlannerTask", "maxActFreqHz", connections.plannerTask.maxActFreq);
		parameter.getString("PlannerTask", "triggerType", connections.plannerTask.trigger);
		if(connections.plannerTask.trigger == "PeriodicTimer") {
			parameter.getDouble("PlannerTask", "periodicActFreqHz", connections.plannerTask.periodicActFreq);
		} else if(connections.plannerTask.trigger == "DataTriggered") {
			parameter.getString("PlannerTask", "inPortRef", connections.plannerTask.inPortRef);
			parameter.getInteger("PlannerTask", "prescale", connections.plannerTask.prescale);
		}
		if(parameter.checkIfParameterExists("PlannerTask", "scheduler")) {
			parameter.getString("PlannerTask", "scheduler", connections.plannerTask.scheduler);
		}
		if(parameter.checkIfParameterExists("PlannerTask", "priority")) {
			parameter.getInteger("PlannerTask", "priority", connections.plannerTask.priority);
		}
		if(parameter.checkIfParameterExists("PlannerTask", "cpuAffinity")) {
			parameter.getInteger("PlannerTask", "cpuAffinity", connections.plannerTask.cpuAffinity);
		}
		
		paramHandler.loadParameter(parameter);
	
	} catch (const SmartACE::IniParameterError & e) {
		std::cerr << e.what() << std::endl;
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std::exception: " << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}
