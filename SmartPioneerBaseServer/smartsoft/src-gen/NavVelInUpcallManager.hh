//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _NAVVELIN_UPCALL_MANAGER_HH
#define _NAVVELIN_UPCALL_MANAGER_HH

#include <list>
#include "aceSmartSoft.hh"
#include "NavVelInUpcallInterface.hh"

/** NavVelInUpcallManager connects input-handling with Upcall propagation
 *
 * This class implements an InputHandler for the InputPort NavVelIn and propagates the handling 
 * of incoming data to all associated (i.e. attached) Upcalls.
 */
class NavVelInUpcallManager
:	public Smart::IInputHandler<CommBasicObjects::CommNavigationVelocity>
{
private:
	// list of associated updalls
	std::list<NavVelInUpcallInterface*> upcalls;

	// call the on_NavVelIn of all the attached NavVelInUpcallInterfaces
	void notify_upcalls(const CommBasicObjects::CommNavigationVelocity &input);
	
protected:
	virtual void handle_input(const CommBasicObjects::CommNavigationVelocity &input) {
		// relay input-handling to all attached NavVelInUpcallInterfaces
		this->notify_upcalls(input);
	}
public:
	NavVelInUpcallManager(
		Smart::InputSubject<CommBasicObjects::CommNavigationVelocity> *subject,
		const int &prescaleFactor=1
	);
	virtual ~NavVelInUpcallManager();
	
	void attach(NavVelInUpcallInterface *upcall);
	void detach(NavVelInUpcallInterface *upcall);
};

#endif
