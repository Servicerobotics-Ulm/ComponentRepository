//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _LOCALIZATIONUPDATE_UPCALL_MANAGER_HH
#define _LOCALIZATIONUPDATE_UPCALL_MANAGER_HH

#include <list>
#include "aceSmartSoft.hh"
#include "LocalizationUpdateUpcallInterface.hh"

/** LocalizationUpdateUpcallManager connects input-handling with Upcall propagation
 *
 * This class implements an InputHandler for the InputPort LocalizationUpdate and propagates the handling 
 * of incoming data to all associated (i.e. attached) Upcalls.
 */
class LocalizationUpdateUpcallManager
:	public Smart::IInputHandler<CommBasicObjects::CommBasePositionUpdate>
{
private:
	// list of associated updalls
	std::list<LocalizationUpdateUpcallInterface*> upcalls;

	// call the on_LocalizationUpdate of all the attached LocalizationUpdateUpcallInterfaces
	void notify_upcalls(const CommBasicObjects::CommBasePositionUpdate &input);
	
protected:
	virtual void handle_input(const CommBasicObjects::CommBasePositionUpdate &input) {
		// relay input-handling to all attached LocalizationUpdateUpcallInterfaces
		this->notify_upcalls(input);
	}
public:
	LocalizationUpdateUpcallManager(
		Smart::InputSubject<CommBasicObjects::CommBasePositionUpdate> *subject,
		const int &prescaleFactor=1
	);
	virtual ~LocalizationUpdateUpcallManager();
	
	void attach(LocalizationUpdateUpcallInterface *upcall);
	void detach(LocalizationUpdateUpcallInterface *upcall);
};

#endif
