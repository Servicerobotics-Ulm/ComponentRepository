//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "LocalizationEventServiceInHandlerCore.hh"
#include "LocalizationEventServiceInHandler.hh"

LocalizationEventServiceInHandlerCore::LocalizationEventServiceInHandlerCore(
	Smart::InputSubject<Smart::EventInputType<CommLocalizationObjects::CommLocalizationEventResult>> *subject,
	const int &prescaleFactor)
	:	Smart::InputTaskTrigger<Smart::EventInputType<CommLocalizationObjects::CommLocalizationEventResult>>(subject, prescaleFactor)
{
}
LocalizationEventServiceInHandlerCore::~LocalizationEventServiceInHandlerCore()
{  
}


void LocalizationEventServiceInHandlerCore::updateAllCommObjects() {
}

void LocalizationEventServiceInHandlerCore::notify_all_interaction_observers() {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	// try dynamically down-casting this class to the derived class 
	// (we can do it safely here as we exactly know the derived class)
	if(const LocalizationEventServiceInHandler* localizationEventServiceInHandler = dynamic_cast<const LocalizationEventServiceInHandler*>(this)) {
		for(auto it=interaction_observers.begin(); it!=interaction_observers.end(); it++) {
			(*it)->on_update_from(localizationEventServiceInHandler);
		}
	}
}

void LocalizationEventServiceInHandlerCore::attach_interaction_observer(LocalizationEventServiceInHandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.push_back(observer);
}

void LocalizationEventServiceInHandlerCore::detach_interaction_observer(LocalizationEventServiceInHandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.remove(observer);
}
