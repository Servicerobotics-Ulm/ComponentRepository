//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ComponentRobotinoImageServer.hh"
#include "smartTimedTaskTrigger.h"
//FIXME: implement logging
//#include "smartGlobalLogger.hh"

// the ace port-factory is used as a default port-mapping
#include "ComponentRobotinoImageServerAcePortFactory.hh"


// initialize static singleton pointer to zero
ComponentRobotinoImageServer* ComponentRobotinoImageServer::_componentRobotinoImageServer = 0;

// constructor
ComponentRobotinoImageServer::ComponentRobotinoImageServer()
{
	std::cout << "constructor of ComponentRobotinoImageServer\n";
	
	// set all pointer members to NULL
	baseStateServiceIn = NULL;
	baseStateServiceInInputTaskTrigger = NULL;
	baseStateServiceInUpcallManager = NULL;
	baseStateServiceInInputCollector = NULL;
	//coordinationPort = NULL;
	//coordinationPort = NULL;
	devicePoseUpdateServiceIn = NULL;
	devicePoseUpdateServiceInInputTaskTrigger = NULL;
	devicePoseUpdateServiceInUpcallManager = NULL;
	devicePoseUpdateServiceInInputCollector = NULL;
	imageTask = NULL;
	imageTaskTrigger = NULL;
	rGBImagePushServiceOut = NULL;
	rGBImagePushServiceOutWrapper = NULL;
	rGBImageQueryServiceAnsw = NULL;
	rGBImageQueryServiceAnswInputTaskTrigger = NULL;
	rGBImageQueryServiceAnswHandler = NULL;
	robotinoAPITask = NULL;
	robotinoAPITaskTrigger = NULL;
	stateChangeHandler = NULL;
	stateSlave = NULL;
	wiringSlave = NULL;
	param = NULL;
	
	// set default ini parameter values
	connections.component.name = "ComponentRobotinoImageServer";
	connections.component.initialComponentMode = "Neutral";
	connections.component.defaultScheduler = "DEFAULT";
	connections.component.useLogger = false;
	
	connections.rGBImagePushServiceOut.serviceName = "RGBImagePushServiceOut";
	connections.rGBImagePushServiceOut.roboticMiddleware = "ACE_SmartSoft";
	connections.rGBImageQueryServiceAnsw.serviceName = "RGBImageQueryServiceAnsw";
	connections.rGBImageQueryServiceAnsw.roboticMiddleware = "ACE_SmartSoft";
	connections.baseStateServiceIn.initialConnect = false;
	connections.baseStateServiceIn.wiringName = "BaseStateServiceIn";
	connections.baseStateServiceIn.serverName = "unknown";
	connections.baseStateServiceIn.serviceName = "unknown";
	connections.baseStateServiceIn.interval = 1;
	connections.baseStateServiceIn.roboticMiddleware = "ACE_SmartSoft";
	connections.devicePoseUpdateServiceIn.initialConnect = false;
	connections.devicePoseUpdateServiceIn.wiringName = "DevicePoseUpdateServiceIn";
	connections.devicePoseUpdateServiceIn.serverName = "unknown";
	connections.devicePoseUpdateServiceIn.serviceName = "unknown";
	connections.devicePoseUpdateServiceIn.interval = 1;
	connections.devicePoseUpdateServiceIn.roboticMiddleware = "ACE_SmartSoft";
	connections.imageTask.minActFreq = 0.0;
	connections.imageTask.maxActFreq = 0.0;
	// scheduling default parameters
	connections.imageTask.scheduler = "DEFAULT";
	connections.imageTask.priority = -1;
	connections.imageTask.cpuAffinity = -1;
	connections.robotinoAPITask.minActFreq = 0.0;
	connections.robotinoAPITask.maxActFreq = 0.0;
	connections.robotinoAPITask.trigger = "PeriodicTimer";
	connections.robotinoAPITask.periodicActFreq = 50.0;
	// scheduling default parameters
	connections.robotinoAPITask.scheduler = "DEFAULT";
	connections.robotinoAPITask.priority = -1;
	connections.robotinoAPITask.cpuAffinity = -1;
	
	// initialize members of ComponentRobotinoImageServerROS1InterfacesExtension
	
	// initialize members of ComponentRobotinoImageServerRestInterfacesExtension
	
	// initialize members of PlainOpcUaComponentRobotinoImageServerExtension
	
}

void ComponentRobotinoImageServer::addPortFactory(const std::string &name, ComponentRobotinoImageServerPortFactoryInterface *portFactory)
{
	portFactoryRegistry[name] = portFactory;
}

void ComponentRobotinoImageServer::addExtension(ComponentRobotinoImageServerExtension *extension)
{
	componentExtensionRegistry[extension->getName()] = extension;
}

SmartACE::SmartComponent* ComponentRobotinoImageServer::getComponentImpl()
{
	return dynamic_cast<ComponentRobotinoImageServerAcePortFactory*>(portFactoryRegistry["ACE_SmartSoft"])->getComponentImpl();
}

/**
 * Notify the component that setup/initialization is finished.
 * You may call this function from anywhere in the component.
 *
 * Set component's internal lifecycle state automaton (if any) into 
 * Alive mode (from here on the component is ready to provide its services)
 */
void ComponentRobotinoImageServer::setStartupFinished() {
	stateSlave->setWaitState("Alive");
	std::cout << "ComponentDefinition initialization/startup finished." << std::endl;
}


Smart::StatusCode ComponentRobotinoImageServer::connectBaseStateServiceIn(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	if(connections.baseStateServiceIn.initialConnect == false) {
		return Smart::SMART_OK;
	}
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = baseStateServiceIn->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->baseStateServiceIn->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	baseStateServiceIn->subscribe(connections.baseStateServiceIn.interval);
	return status;
}
Smart::StatusCode ComponentRobotinoImageServer::connectDevicePoseUpdateServiceIn(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	if(connections.devicePoseUpdateServiceIn.initialConnect == false) {
		return Smart::SMART_OK;
	}
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = devicePoseUpdateServiceIn->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->devicePoseUpdateServiceIn->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	devicePoseUpdateServiceIn->subscribe(connections.devicePoseUpdateServiceIn.interval);
	return status;
}


/**
 * First connect ALL client ports contained in this component, then start all services:
 * activate state, push, etc...
 */
Smart::StatusCode ComponentRobotinoImageServer::connectAndStartAllServices() {
	Smart::StatusCode status = Smart::SMART_OK;
	
	status = connectBaseStateServiceIn(connections.baseStateServiceIn.serverName, connections.baseStateServiceIn.serviceName);
	if(status != Smart::SMART_OK) return status;
	status = connectDevicePoseUpdateServiceIn(connections.devicePoseUpdateServiceIn.serverName, connections.devicePoseUpdateServiceIn.serviceName);
	if(status != Smart::SMART_OK) return status;
	return status;
}

/**
 * Start all tasks contained in this component.
 */
void ComponentRobotinoImageServer::startAllTasks() {
	// start task ImageTask
	if(connections.imageTask.scheduler != "DEFAULT") {
		ACE_Sched_Params imageTask_SchedParams(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(connections.imageTask.scheduler == "FIFO") {
			imageTask_SchedParams.policy(ACE_SCHED_FIFO);
			imageTask_SchedParams.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(connections.imageTask.scheduler == "RR") {
			imageTask_SchedParams.policy(ACE_SCHED_RR);
			imageTask_SchedParams.priority(ACE_THR_PRI_RR_MIN);
		}
		imageTask->start(imageTask_SchedParams, connections.imageTask.cpuAffinity);
	} else {
		imageTask->start();
	}
	// start task robotinoAPITask
	if(connections.robotinoAPITask.scheduler != "DEFAULT") {
		ACE_Sched_Params robotinoAPITask_SchedParams(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(connections.robotinoAPITask.scheduler == "FIFO") {
			robotinoAPITask_SchedParams.policy(ACE_SCHED_FIFO);
			robotinoAPITask_SchedParams.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(connections.robotinoAPITask.scheduler == "RR") {
			robotinoAPITask_SchedParams.policy(ACE_SCHED_RR);
			robotinoAPITask_SchedParams.priority(ACE_THR_PRI_RR_MIN);
		}
		robotinoAPITask->start(robotinoAPITask_SchedParams, connections.robotinoAPITask.cpuAffinity);
	} else {
		robotinoAPITask->start();
	}
}

/**
 * Start all timers contained in this component
 */
void ComponentRobotinoImageServer::startAllTimers() {
}


Smart::TaskTriggerSubject* ComponentRobotinoImageServer::getInputTaskTriggerFromString(const std::string &client)
{
	if(client == "BaseStateServiceIn") return baseStateServiceInInputTaskTrigger;
	if(client == "DevicePoseUpdateServiceIn") return devicePoseUpdateServiceInInputTaskTrigger;
	
	return NULL;
}


void ComponentRobotinoImageServer::init(int argc, char *argv[])
{
	try {
		Smart::StatusCode status;
		
		// load initial parameters from ini-file (if found)
		loadParameter(argc, argv);
		
		// print out the actual parameters which are used to initialize the component
		std::cout << " \nComponentDefinition Initial-Parameters:\n" << COMP->getParameters() << std::endl;
		
		// initializations of ComponentRobotinoImageServerROS1InterfacesExtension
		
		// initializations of ComponentRobotinoImageServerRestInterfacesExtension
		
		// initializations of PlainOpcUaComponentRobotinoImageServerExtension
		
		
		// initialize all registered port-factories
		for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
		{
			portFactory->second->initialize(this, argc, argv);
		}
		
		// initialize all registered component-extensions
		for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
		{
			extension->second->initialize(this, argc, argv);
		}
		
		ComponentRobotinoImageServerPortFactoryInterface *acePortFactory = portFactoryRegistry["ACE_SmartSoft"];
		if(acePortFactory == 0) {
			std::cerr << "ERROR: acePortFactory NOT instantiated -> exit(-1)" << std::endl;
			exit(-1);
		}
		
		// this pointer is used for backwards compatibility (deprecated: should be removed as soon as all patterns, including coordination, are moved to port-factory)
		SmartACE::SmartComponent *component = dynamic_cast<ComponentRobotinoImageServerAcePortFactory*>(acePortFactory)->getComponentImpl();
		
		std::cout << "ComponentDefinition ComponentRobotinoImageServer is named " << connections.component.name << std::endl;
		
		if(connections.component.useLogger == true) {
			//FIXME: use logging
			//Smart::LOGGER->openLogFileInFolder("data/"+connections.component.name);
			//Smart::LOGGER->startLogging();
		}

		// create event-test handlers (if needed)
		
		// create server ports
		// TODO: set minCycleTime from Ini-file
		rGBImagePushServiceOut = portFactoryRegistry[connections.rGBImagePushServiceOut.roboticMiddleware]->createRGBImagePushServiceOut(connections.rGBImagePushServiceOut.serviceName);
		rGBImagePushServiceOutWrapper = new RGBImagePushServiceOutWrapper(rGBImagePushServiceOut);
		rGBImageQueryServiceAnsw = portFactoryRegistry[connections.rGBImageQueryServiceAnsw.roboticMiddleware]->createRGBImageQueryServiceAnsw(connections.rGBImageQueryServiceAnsw.serviceName);
		rGBImageQueryServiceAnswInputTaskTrigger = new Smart::QueryServerTaskTrigger<CommBasicObjects::CommVoid, DomainVision::CommVideoImage>(rGBImageQueryServiceAnsw);
		
		// create client ports
		baseStateServiceIn = portFactoryRegistry[connections.baseStateServiceIn.roboticMiddleware]->createBaseStateServiceIn();
		devicePoseUpdateServiceIn = portFactoryRegistry[connections.devicePoseUpdateServiceIn.roboticMiddleware]->createDevicePoseUpdateServiceIn();
		
		// create InputTaskTriggers and UpcallManagers
		baseStateServiceInInputCollector = new BaseStateServiceInInputCollector(baseStateServiceIn);
		baseStateServiceInInputTaskTrigger = new Smart::InputTaskTrigger<CommBasicObjects::CommBaseState>(baseStateServiceInInputCollector);
		baseStateServiceInUpcallManager = new BaseStateServiceInUpcallManager(baseStateServiceInInputCollector);
		devicePoseUpdateServiceInInputCollector = new DevicePoseUpdateServiceInInputCollector(devicePoseUpdateServiceIn);
		devicePoseUpdateServiceInInputTaskTrigger = new Smart::InputTaskTrigger<CommBasicObjects::CommDevicePoseState>(devicePoseUpdateServiceInInputCollector);
		devicePoseUpdateServiceInUpcallManager = new DevicePoseUpdateServiceInUpcallManager(devicePoseUpdateServiceInInputCollector);
		
		// create input-handler
		
		// create request-handlers
		rGBImageQueryServiceAnswHandler = new RGBImageQueryServiceAnswHandler(rGBImageQueryServiceAnsw);
		
		// create state pattern
		stateChangeHandler = new SmartStateChangeHandler();
		stateSlave = new SmartACE::StateSlave(component, stateChangeHandler);
		if (stateSlave->defineStates("PushImage" ,"pushimage") != Smart::SMART_OK) std::cerr << "ERROR: defining state combinaion PushImage.pushimage" << std::endl;
		if (stateSlave->defineStates("QueryImage" ,"queryimage") != Smart::SMART_OK) std::cerr << "ERROR: defining state combinaion QueryImage.queryimage" << std::endl;
		status = stateSlave->setUpInitialState(connections.component.initialComponentMode);
		if (status != Smart::SMART_OK) std::cerr << status << "; failed setting initial ComponentMode: " << connections.component.initialComponentMode << std::endl;
		// activate state slave
		status = stateSlave->activate();
		if(status != Smart::SMART_OK) std::cerr << "ERROR: activate state" << std::endl;
		
		wiringSlave = new SmartACE::WiringSlave(component);
		// add client port to wiring slave
		if(connections.baseStateServiceIn.roboticMiddleware == "ACE_SmartSoft") {
			//FIXME: this must also work with other implementations
			dynamic_cast<SmartACE::PushClient<CommBasicObjects::CommBaseState>*>(baseStateServiceIn)->add(wiringSlave, connections.baseStateServiceIn.wiringName);
		}
		if(connections.devicePoseUpdateServiceIn.roboticMiddleware == "ACE_SmartSoft") {
			//FIXME: this must also work with other implementations
			dynamic_cast<SmartACE::PushClient<CommBasicObjects::CommDevicePoseState>*>(devicePoseUpdateServiceIn)->add(wiringSlave, connections.devicePoseUpdateServiceIn.wiringName);
		}
		
		// create parameter slave
		param = new SmartACE::ParameterSlave(component, &paramHandler);
		
		
		// create Task ImageTask
		imageTask = new ImageTask(component);
		// configure input-links
		// configure task-trigger (if task is configurable)
		if(connections.imageTask.trigger == "PeriodicTimer") {
			// create PeriodicTimerTrigger
			int microseconds = 1000*1000 / connections.imageTask.periodicActFreq;
			if(microseconds > 0) {
				Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
				triggerPtr->attach(imageTask);
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				// store trigger in class member
				imageTaskTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task ImageTask" << std::endl;
			}
		} else if(connections.imageTask.trigger == "DataTriggered") {
			imageTaskTrigger = getInputTaskTriggerFromString(connections.imageTask.inPortRef);
			if(imageTaskTrigger != NULL) {
				imageTaskTrigger->attach(imageTask, connections.imageTask.prescale);
			} else {
				std::cerr << "ERROR: could not set-up InPort " << connections.imageTask.inPortRef << " as activation source for Task ImageTask" << std::endl;
			}
		} 
		
		// create Task robotinoAPITask
		robotinoAPITask = new RobotinoAPITask(component);
		// configure input-links
		// configure task-trigger (if task is configurable)
		if(connections.robotinoAPITask.trigger == "PeriodicTimer") {
			// create PeriodicTimerTrigger
			int microseconds = 1000*1000 / connections.robotinoAPITask.periodicActFreq;
			if(microseconds > 0) {
				Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
				triggerPtr->attach(robotinoAPITask);
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				// store trigger in class member
				robotinoAPITaskTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task RobotinoAPITask" << std::endl;
			}
		} else if(connections.robotinoAPITask.trigger == "DataTriggered") {
			robotinoAPITaskTrigger = getInputTaskTriggerFromString(connections.robotinoAPITask.inPortRef);
			if(robotinoAPITaskTrigger != NULL) {
				robotinoAPITaskTrigger->attach(robotinoAPITask, connections.robotinoAPITask.prescale);
			} else {
				std::cerr << "ERROR: could not set-up InPort " << connections.robotinoAPITask.inPortRef << " as activation source for Task RobotinoAPITask" << std::endl;
			}
		} else
		{
			// setup default task-trigger as PeriodicTimer
			Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
			int microseconds = 1000*1000 / 50.0;
			if(microseconds > 0) {
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				triggerPtr->attach(robotinoAPITask);
				// store trigger in class member
				robotinoAPITaskTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task RobotinoAPITask" << std::endl;
			}
		}
		
		
		// link observers with subjects
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std exception" << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}

// run the component
void ComponentRobotinoImageServer::run()
{
	stateSlave->acquire("init");
	// startup all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->onStartup();
	}
	
	// startup all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->onStartup();
	}
	stateSlave->release("init");
	
	// do not call this handler within the init state (see above) as this handler internally calls setStartupFinished() (this should be fixed in future)
	compHandler.onStartup();
	
	// this call blocks until the component is commanded to shutdown
	stateSlave->acquire("shutdown");
	
	// shutdown all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->onShutdown();
	}
	
	// shutdown all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->onShutdown();
	}
	
	if(connections.component.useLogger == true) {
		//FIXME: use logging
		//Smart::LOGGER->stopLogging();
	}
	
	compHandler.onShutdown();
	
	stateSlave->release("shutdown");
}

// clean-up component's resources
void ComponentRobotinoImageServer::fini()
{
	// unlink all observers
	
	// destroy all task instances
	// unlink all UpcallManagers
	// unlink the TaskTrigger
	if(imageTaskTrigger != NULL){
		imageTaskTrigger->detach(imageTask);
		delete imageTask;
	}
	// unlink all UpcallManagers
	// unlink the TaskTrigger
	if(robotinoAPITaskTrigger != NULL){
		robotinoAPITaskTrigger->detach(robotinoAPITask);
		delete robotinoAPITask;
	}

	// destroy all input-handler

	// destroy InputTaskTriggers and UpcallManagers
	delete baseStateServiceInInputTaskTrigger;
	delete baseStateServiceInUpcallManager;
	delete baseStateServiceInInputCollector;
	delete devicePoseUpdateServiceInInputTaskTrigger;
	delete devicePoseUpdateServiceInUpcallManager;
	delete devicePoseUpdateServiceInInputCollector;

	// destroy client ports
	delete baseStateServiceIn;
	delete devicePoseUpdateServiceIn;

	// destroy server ports
	delete rGBImagePushServiceOutWrapper;
	delete rGBImagePushServiceOut;
	delete rGBImageQueryServiceAnsw;
	delete rGBImageQueryServiceAnswInputTaskTrigger;
	// destroy event-test handlers (if needed)
	
	// destroy request-handlers
	delete rGBImageQueryServiceAnswHandler;
	
	delete stateSlave;
	// destroy state-change-handler
	delete stateChangeHandler;
	
	// destroy all master/slave ports
	delete wiringSlave;
	delete param;
	

	// destroy all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->destroy();
	}

	// destroy all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->destroy();
	}
	
	// destruction of ComponentRobotinoImageServerROS1InterfacesExtension
	
	// destruction of ComponentRobotinoImageServerRestInterfacesExtension
	
	// destruction of PlainOpcUaComponentRobotinoImageServerExtension
	
}

void ComponentRobotinoImageServer::loadParameter(int argc, char *argv[])
{
	/*
	 Parameters can be specified via command line --filename=<filename> or -f <filename>

	 With this parameter present:
	   - The component will look for the file in the current working directory,
	     a path relative to the current directory or any absolute path
	   - The component will use the default values if the file cannot be found

	 With this parameter absent:
	   - <Name of Component>.ini will be read from current working directory, if found there
	   - $SMART_ROOT/etc/<Name of Component>.ini will be read otherwise
	   - Default values will be used if neither found in working directory or /etc
	 */
	SmartACE::SmartIniParameter parameter;
	std::ifstream parameterfile;
	bool parameterFileFound = false;

	// load parameters
	try
	{
		// if paramfile is given as argument
		if(parameter.tryAddFileFromArgs(argc,argv,"filename", 'f'))
		{
			parameterFileFound = true;
			std::cout << "parameter file is loaded from an argv argument \n";
		} else if(parameter.searchFile("ComponentRobotinoImageServer.ini", parameterfile)) {
			parameterFileFound = true;
			std::cout << "load ComponentRobotinoImageServer.ini parameter file\n";
			parameter.addFile(parameterfile);
		} else {
			std::cout << "WARNING: ComponentRobotinoImageServer.ini parameter file not found! (using default values or command line arguments)\n";
		}
		
		// add command line arguments to allow overwriting of parameters
		// from file
		parameter.addCommandLineArgs(argc,argv,"component");
		
		// initialize the naming service using the command line parameters parsed in the
		// SmartIniParameter class. The naming service parameters are expected to be in
		// the "component" parameter group.
		SmartACE::NAMING::instance()->checkForHelpArg(argc,argv);
		if(parameterFileFound) 
		{
			if(SmartACE::NAMING::instance()->init(parameter.getAllParametersFromGroup("component")) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		} else {
			if(SmartACE::NAMING::instance()->init(argc, argv) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		}
			
		// print all known parameters
		// parameter.print();
		
		//--- server port // client port // other parameter ---
		// load parameter
		parameter.getString("component", "name", connections.component.name);
		parameter.getString("component", "initialComponentMode", connections.component.initialComponentMode);
		if(parameter.checkIfParameterExists("component", "defaultScheduler")) {
			parameter.getString("component", "defaultScheduler", connections.component.defaultScheduler);
		}
		if(parameter.checkIfParameterExists("component", "useLogger")) {
			parameter.getBoolean("component", "useLogger", connections.component.useLogger);
		}
		
		// load parameters for client BaseStateServiceIn
		parameter.getBoolean("BaseStateServiceIn", "initialConnect", connections.baseStateServiceIn.initialConnect);
		parameter.getString("BaseStateServiceIn", "serviceName", connections.baseStateServiceIn.serviceName);
		parameter.getString("BaseStateServiceIn", "serverName", connections.baseStateServiceIn.serverName);
		parameter.getString("BaseStateServiceIn", "wiringName", connections.baseStateServiceIn.wiringName);
		parameter.getInteger("BaseStateServiceIn", "interval", connections.baseStateServiceIn.interval);
		if(parameter.checkIfParameterExists("BaseStateServiceIn", "roboticMiddleware")) {
			parameter.getString("BaseStateServiceIn", "roboticMiddleware", connections.baseStateServiceIn.roboticMiddleware);
		}
		// load parameters for client DevicePoseUpdateServiceIn
		parameter.getBoolean("DevicePoseUpdateServiceIn", "initialConnect", connections.devicePoseUpdateServiceIn.initialConnect);
		parameter.getString("DevicePoseUpdateServiceIn", "serviceName", connections.devicePoseUpdateServiceIn.serviceName);
		parameter.getString("DevicePoseUpdateServiceIn", "serverName", connections.devicePoseUpdateServiceIn.serverName);
		parameter.getString("DevicePoseUpdateServiceIn", "wiringName", connections.devicePoseUpdateServiceIn.wiringName);
		parameter.getInteger("DevicePoseUpdateServiceIn", "interval", connections.devicePoseUpdateServiceIn.interval);
		if(parameter.checkIfParameterExists("DevicePoseUpdateServiceIn", "roboticMiddleware")) {
			parameter.getString("DevicePoseUpdateServiceIn", "roboticMiddleware", connections.devicePoseUpdateServiceIn.roboticMiddleware);
		}
		
		// load parameters for server RGBImagePushServiceOut
		parameter.getString("RGBImagePushServiceOut", "serviceName", connections.rGBImagePushServiceOut.serviceName);
		if(parameter.checkIfParameterExists("RGBImagePushServiceOut", "roboticMiddleware")) {
			parameter.getString("RGBImagePushServiceOut", "roboticMiddleware", connections.rGBImagePushServiceOut.roboticMiddleware);
		}
		// load parameters for server RGBImageQueryServiceAnsw
		parameter.getString("RGBImageQueryServiceAnsw", "serviceName", connections.rGBImageQueryServiceAnsw.serviceName);
		if(parameter.checkIfParameterExists("RGBImageQueryServiceAnsw", "roboticMiddleware")) {
			parameter.getString("RGBImageQueryServiceAnsw", "roboticMiddleware", connections.rGBImageQueryServiceAnsw.roboticMiddleware);
		}
		
		// load parameters for task ImageTask
		parameter.getDouble("ImageTask", "minActFreqHz", connections.imageTask.minActFreq);
		parameter.getDouble("ImageTask", "maxActFreqHz", connections.imageTask.maxActFreq);
		parameter.getString("ImageTask", "triggerType", connections.imageTask.trigger);
		if(connections.imageTask.trigger == "PeriodicTimer") {
			parameter.getDouble("ImageTask", "periodicActFreqHz", connections.imageTask.periodicActFreq);
		} else if(connections.imageTask.trigger == "DataTriggered") {
			parameter.getString("ImageTask", "inPortRef", connections.imageTask.inPortRef);
			parameter.getInteger("ImageTask", "prescale", connections.imageTask.prescale);
		}
		if(parameter.checkIfParameterExists("ImageTask", "scheduler")) {
			parameter.getString("ImageTask", "scheduler", connections.imageTask.scheduler);
		}
		if(parameter.checkIfParameterExists("ImageTask", "priority")) {
			parameter.getInteger("ImageTask", "priority", connections.imageTask.priority);
		}
		if(parameter.checkIfParameterExists("ImageTask", "cpuAffinity")) {
			parameter.getInteger("ImageTask", "cpuAffinity", connections.imageTask.cpuAffinity);
		}
		// load parameters for task robotinoAPITask
		parameter.getDouble("robotinoAPITask", "minActFreqHz", connections.robotinoAPITask.minActFreq);
		parameter.getDouble("robotinoAPITask", "maxActFreqHz", connections.robotinoAPITask.maxActFreq);
		parameter.getString("robotinoAPITask", "triggerType", connections.robotinoAPITask.trigger);
		if(connections.robotinoAPITask.trigger == "PeriodicTimer") {
			parameter.getDouble("robotinoAPITask", "periodicActFreqHz", connections.robotinoAPITask.periodicActFreq);
		} else if(connections.robotinoAPITask.trigger == "DataTriggered") {
			parameter.getString("robotinoAPITask", "inPortRef", connections.robotinoAPITask.inPortRef);
			parameter.getInteger("robotinoAPITask", "prescale", connections.robotinoAPITask.prescale);
		}
		if(parameter.checkIfParameterExists("robotinoAPITask", "scheduler")) {
			parameter.getString("robotinoAPITask", "scheduler", connections.robotinoAPITask.scheduler);
		}
		if(parameter.checkIfParameterExists("robotinoAPITask", "priority")) {
			parameter.getInteger("robotinoAPITask", "priority", connections.robotinoAPITask.priority);
		}
		if(parameter.checkIfParameterExists("robotinoAPITask", "cpuAffinity")) {
			parameter.getInteger("robotinoAPITask", "cpuAffinity", connections.robotinoAPITask.cpuAffinity);
		}
		
		// load parameters for ComponentRobotinoImageServerROS1InterfacesExtension
		
		// load parameters for ComponentRobotinoImageServerRestInterfacesExtension
		
		// load parameters for PlainOpcUaComponentRobotinoImageServerExtension
		
		
		// load parameters for all registered component-extensions
		for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
		{
			extension->second->loadParameters(parameter);
		}
		
		paramHandler.loadParameter(parameter);
	
	} catch (const SmartACE::IniParameterError & e) {
		std::cerr << e.what() << std::endl;
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std::exception: " << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}
