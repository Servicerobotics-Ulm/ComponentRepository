//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _COMPONENTREALSENSEPERSONTRACKER_HH
#define _COMPONENTREALSENSEPERSONTRACKER_HH

#include <map>
#include <iostream>
#include "aceSmartSoft.hh"
#include "smartQueryServerTaskTrigger_T.h"
#include "ComponentRealSensePersonTrackerCore.hh"

#include "ComponentRealSensePersonTrackerPortFactoryInterface.hh"
#include "ComponentRealSensePersonTrackerExtension.hh"

// forward declarations
class ComponentRealSensePersonTrackerPortFactoryInterface;
class ComponentRealSensePersonTrackerExtension;

// includes for PlainOpcUaComponentRealSensePersonTrackerExtension
// include plain OPC UA device clients
// include plain OPC UA status servers


// include communication objects
#include <CommTrackingObjects/CommPersonLostEventParameter.hh>
#include <CommTrackingObjects/CommPersonLostEventParameterACE.hh>
#include <CommTrackingObjects/CommPersonLostEventResult.hh>
#include <CommTrackingObjects/CommPersonLostEventResultACE.hh>
#include <DomainVision/CommRGBDImage.hh>
#include <DomainVision/CommRGBDImageACE.hh>
#include <CommTrackingObjects/CommTrackingGoal.hh>
#include <CommTrackingObjects/CommTrackingGoalACE.hh>
#include <DomainVision/CommVideoImage.hh>
#include <DomainVision/CommVideoImageACE.hh>
#include <CommTrackingObjects/PersonLostEventState.hh>
#include <CommTrackingObjects/PersonLostEventStateACE.hh>

// include tasks
#include "TrackingThread.hh"
// include UpcallManagers and InputCollectors
#include "RealSenseClientUpcallManager.hh"
#include "RealSenseClientInputCollector.hh"

// include input-handler(s)
// include request-handler(s)
// output port wrappers
#include "Rgb_video_serverWrapper.hh"
#include "TrackingGoalServerWrapper.hh"
#include "PersonLostEventServerWrapper.hh"

// include handler
#include "CompHandler.hh"

#include "ParameterStateStruct.hh"
#include "ParameterUpdateHandler.hh"

#include "SmartStateChangeHandler.hh"
#include "StateActivityManager.hh"


#define COMP ComponentRealSensePersonTracker::instance()

class ComponentRealSensePersonTracker : public ComponentRealSensePersonTrackerCore {
private:
	static ComponentRealSensePersonTracker *_componentRealSensePersonTracker;
	
	// constructor
	ComponentRealSensePersonTracker();
	
	// copy-constructor
	ComponentRealSensePersonTracker(const ComponentRealSensePersonTracker& cc);
	
	// destructor
	~ComponentRealSensePersonTracker() { };
	
	// load parameter from ini file
	void loadParameter(int argc, char* argv[]);
	
	// instantiate comp-handler
	CompHandler compHandler;
	
	// helper method that maps a string-name to an according TaskTriggerSubject
	Smart::TaskTriggerSubject* getInputTaskTriggerFromString(const std::string &client);
	
	// internal map storing the different port-creation factories (that internally map to specific middleware implementations)
	std::map<std::string, ComponentRealSensePersonTrackerPortFactoryInterface*> portFactoryRegistry;
	
	// internal map storing various extensions of this component class
	std::map<std::string, ComponentRealSensePersonTrackerExtension*> componentExtensionRegistry;
	
public:
	ParameterStateStruct getGlobalState() const
	{
		return paramHandler.getGlobalState();
	}
	
	ParameterStateStruct getParameters() const
	{
		return paramHandler.getGlobalState();
	}
	
	// define tasks
	Smart::TaskTriggerSubject* trackingThreadTrigger;
	TrackingThread *trackingThread;
	
	// define input-ports
	// InputPort RealSenseClient
	Smart::IPushClientPattern<DomainVision::CommRGBDImage> *realSenseClient;
	Smart::InputTaskTrigger<DomainVision::CommRGBDImage> *realSenseClientInputTaskTrigger;
	RealSenseClientUpcallManager *realSenseClientUpcallManager;
	RealSenseClientInputCollector *realSenseClientInputCollector;
	
	// define request-ports
	
	// define input-handler
	
	// define output-ports
	Smart::IEventServerPattern<CommTrackingObjects::CommPersonLostEventParameter, CommTrackingObjects::CommPersonLostEventResult, CommTrackingObjects::PersonLostEventState> *personLostEventServer;
	PersonLostEventServerWrapper *personLostEventServerWrapper;
	std::shared_ptr<Smart::IEventTestHandler<CommTrackingObjects::CommPersonLostEventParameter, CommTrackingObjects::CommPersonLostEventResult, CommTrackingObjects::PersonLostEventState>> personLostEventServerEventTestHandler;
	Smart::IPushServerPattern<DomainVision::CommVideoImage> *rgb_video_server;
	Rgb_video_serverWrapper *rgb_video_serverWrapper;
	Smart::IPushServerPattern<CommTrackingObjects::CommTrackingGoal> *trackingGoalServer;
	TrackingGoalServerWrapper *trackingGoalServerWrapper;
	
	// define answer-ports
	
	// define request-handlers
	
	
	// define default slave ports
	SmartACE::StateSlave *stateSlave;
	StateActivityManager *stateActivityManager;
	SmartStateChangeHandler *stateChangeHandler;
	SmartACE::WiringSlave *wiringSlave;
	ParamUpdateHandler paramHandler;
	SmartACE::ParameterSlave *param;
	
	
	/// this method is used to register different PortFactory classes (one for each supported middleware framework)
	void addPortFactory(const std::string &name, ComponentRealSensePersonTrackerPortFactoryInterface *portFactory);
	
	SmartACE::SmartComponent* getComponentImpl();
	
	/// this method is used to register different component-extension classes
	void addExtension(ComponentRealSensePersonTrackerExtension *extension);
	
	/// this method allows to access the registered component-extensions (automatically converting to the actuall implementation type)
	template <typename T>
	T* getExtension(const std::string &name) {
		auto it = componentExtensionRegistry.find(name);
		if(it != componentExtensionRegistry.end()) {
			return dynamic_cast<T*>(it->second);
		}
		return 0;
	}
	
	/// initialize component's internal members
	void init(int argc, char *argv[]);
	
	/// execute the component's infrastructure
	void run();
	
	/// clean-up component's resources
	void fini();
	
	/// call this method to set the overall component into the Alive state (i.e. component is then ready to operate)
	void setStartupFinished();
	
	/// connect all component's client ports
	Smart::StatusCode connectAndStartAllServices();
	
	/// start all assocuated Activities
	void startAllTasks();
	
	/// start all associated timers
	void startAllTimers();
	
	Smart::StatusCode connectRealSenseClient(const std::string &serverName, const std::string &serviceName);

	// return singleton instance
	static ComponentRealSensePersonTracker* instance()
	{
		if(_componentRealSensePersonTracker == 0) {
			_componentRealSensePersonTracker = new ComponentRealSensePersonTracker();
		}
		return _componentRealSensePersonTracker;
	}
	
	static void deleteInstance() {
		if(_componentRealSensePersonTracker != 0) {
			delete _componentRealSensePersonTracker;
		}
	}
	
	// connections parameter
	struct connections_struct
	{
		// component struct
		struct component_struct
		{
			// the name of the component
			std::string name;
			std::string initialComponentMode;
			std::string defaultScheduler;
			bool useLogger;
		} component;
		
		//--- task parameter ---
		struct TrackingThread_struct {
			double minActFreq;
			double maxActFreq;
			std::string trigger;
			// only one of the following two params is 
			// actually used at run-time according 
			// to the system config model
			double periodicActFreq;
			// or
			std::string inPortRef;
			int prescale;
			// scheduling parameters
			std::string scheduler;
			int priority;
			int cpuAffinity;
		} trackingThread;
		
		//--- upcall parameter ---
		
		//--- server port parameter ---
		struct PersonLostEventServer_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} personLostEventServer;
		struct Rgb_video_server_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} rgb_video_server;
		struct TrackingGoalServer_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} trackingGoalServer;
	
		//--- client port parameter ---
		struct RealSenseClient_struct {
			bool initialConnect;
			std::string serverName;
			std::string serviceName;
			std::string wiringName;
			long interval;
			std::string roboticMiddleware;
		} realSenseClient;
		
	} connections;
};
#endif
