//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _NAVVELSENDSERVER_UPCALL_MANAGER_HH
#define _NAVVELSENDSERVER_UPCALL_MANAGER_HH

#include <list>
#include "aceSmartSoft.hh"
#include "NavVelSendServerUpcallInterface.hh"

/** NavVelSendServerUpcallManager connects input-handling with Upcall propagation
 *
 * This class implements an InputHandler for the InputPort NavVelSendServer and propagates the handling 
 * of incoming data to all associated (i.e. attached) Upcalls.
 */
class NavVelSendServerUpcallManager
:	public Smart::IInputHandler<CommBasicObjects::CommNavigationVelocity>
{
private:
	// list of associated updalls
	std::list<NavVelSendServerUpcallInterface*> upcalls;

	// call the on_NavVelSendServer of all the attached NavVelSendServerUpcallInterfaces
	void notify_upcalls(const CommBasicObjects::CommNavigationVelocity &input);
	
protected:
	virtual void handle_input(const CommBasicObjects::CommNavigationVelocity &input) {
		// relay input-handling to all attached NavVelSendServerUpcallInterfaces
		this->notify_upcalls(input);
	}
public:
	NavVelSendServerUpcallManager(
		Smart::InputSubject<CommBasicObjects::CommNavigationVelocity> *subject,
		const int &prescaleFactor=1
	);
	virtual ~NavVelSendServerUpcallManager();
	
	void attach(NavVelSendServerUpcallInterface *upcall);
	void detach(NavVelSendServerUpcallInterface *upcall);
};

#endif
