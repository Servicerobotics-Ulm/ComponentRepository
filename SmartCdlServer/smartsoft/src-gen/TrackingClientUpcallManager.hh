//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _TRACKINGCLIENT_UPCALL_MANAGER_HH
#define _TRACKINGCLIENT_UPCALL_MANAGER_HH

#include <list>
#include "aceSmartSoft.hh"
#include "TrackingClientUpcallInterface.hh"

/** TrackingClientUpcallManager connects input-handling with Upcall propagation
 *
 * This class implements an InputHandler for the InputPort TrackingClient and propagates the handling 
 * of incoming data to all associated (i.e. attached) Upcalls.
 */
class TrackingClientUpcallManager
:	public Smart::IInputHandler<CommTrackingObjects::CommTrackingGoal>
{
private:
	// list of associated updalls
	std::list<TrackingClientUpcallInterface*> upcalls;

	// call the on_TrackingClient of all the attached TrackingClientUpcallInterfaces
	void notify_upcalls(const CommTrackingObjects::CommTrackingGoal &input);
	
protected:
	virtual void handle_input(const CommTrackingObjects::CommTrackingGoal &input) {
		// relay input-handling to all attached TrackingClientUpcallInterfaces
		this->notify_upcalls(input);
	}
public:
	TrackingClientUpcallManager(
		Smart::InputSubject<CommTrackingObjects::CommTrackingGoal> *subject,
		const int &prescaleFactor=1
	);
	virtual ~TrackingClientUpcallManager();
	
	void attach(TrackingClientUpcallInterface *upcall);
	void detach(TrackingClientUpcallInterface *upcall);
};

#endif
