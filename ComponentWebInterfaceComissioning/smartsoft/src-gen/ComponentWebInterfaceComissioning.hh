//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _COMPONENTWEBINTERFACECOMISSIONING_HH
#define _COMPONENTWEBINTERFACECOMISSIONING_HH

#include <map>
#include <iostream>
#include "aceSmartSoft.hh"
#include "smartQueryServerTaskTrigger_T.h"
#include "ComponentWebInterfaceComissioningCore.hh"

#include "ComponentWebInterfaceComissioningPortFactoryInterface.hh"
#include "ComponentWebInterfaceComissioningExtension.hh"

// forward declarations
class ComponentWebInterfaceComissioningPortFactoryInterface;
class ComponentWebInterfaceComissioningExtension;

// includes for PlainOpcUaComponentWebInterfaceComissioningExtension
// include plain OPC UA device clients
// include plain OPC UA status servers


// include communication objects
#include <CommBasicObjects/CommSkillMsg.hh>
#include <CommBasicObjects/CommSkillMsgACE.hh>

// include tasks
#include "DataTask.hh"
#include "WorkerTask.hh"
// include UpcallManagers and InputCollectors
#include "SequencerSendServerUpcallManager.hh"
#include "SequencerSendServerInputCollector.hh"

// include input-handler(s)
#include "SendHandler.hh"
// include request-handler(s)
// output port wrappers
#include "SequencerEventServerWrapper.hh"

// include handler
#include "CompHandler.hh"

#include "ParameterStateStruct.hh"
#include "ParameterUpdateHandler.hh"

#include "SmartStateChangeHandler.hh"
#include "StateActivityManager.hh"


#define COMP ComponentWebInterfaceComissioning::instance()

class ComponentWebInterfaceComissioning : public ComponentWebInterfaceComissioningCore {
private:
	static ComponentWebInterfaceComissioning *_componentWebInterfaceComissioning;
	
	// constructor
	ComponentWebInterfaceComissioning();
	
	// copy-constructor
	ComponentWebInterfaceComissioning(const ComponentWebInterfaceComissioning& cc);
	
	// destructor
	~ComponentWebInterfaceComissioning() { };
	
	// load parameter from ini file
	void loadParameter(int argc, char* argv[]);
	
	// instantiate comp-handler
	CompHandler compHandler;
	
	// helper method that maps a string-name to an according TaskTriggerSubject
	Smart::TaskTriggerSubject* getInputTaskTriggerFromString(const std::string &client);
	
	// internal map storing the different port-creation factories (that internally map to specific middleware implementations)
	std::map<std::string, ComponentWebInterfaceComissioningPortFactoryInterface*> portFactoryRegistry;
	
	// internal map storing various extensions of this component class
	std::map<std::string, ComponentWebInterfaceComissioningExtension*> componentExtensionRegistry;
	
public:
	ParameterStateStruct getGlobalState() const
	{
		return paramHandler.getGlobalState();
	}
	
	ParameterStateStruct getParameters() const
	{
		return paramHandler.getGlobalState();
	}
	
	// define tasks
	Smart::TaskTriggerSubject* dataTaskTrigger;
	DataTask *dataTask;
	Smart::TaskTriggerSubject* workerTaskTrigger;
	WorkerTask *workerTask;
	
	// define input-ports
	// InputPort sequencerSendServer
	Smart::ISendServerPattern<CommBasicObjects::CommSkillMsg> *sequencerSendServer;
	Smart::InputTaskTrigger<CommBasicObjects::CommSkillMsg> *sequencerSendServerInputTaskTrigger;
	SequencerSendServerUpcallManager *sequencerSendServerUpcallManager;
	SequencerSendServerInputCollector *sequencerSendServerInputCollector;
	
	// define request-ports
	
	// define input-handler
	SendHandler *sendHandler;
	
	// define output-ports
	Smart::IEventServerPattern<CommBasicObjects::CommSkillMsg, CommBasicObjects::CommSkillMsg, CommBasicObjects::CommSkillMsg> *sequencerEventServer;
	SequencerEventServerWrapper *sequencerEventServerWrapper;
	std::shared_ptr<Smart::IEventTestHandler<CommBasicObjects::CommSkillMsg, CommBasicObjects::CommSkillMsg, CommBasicObjects::CommSkillMsg>> sequencerEventServerEventTestHandler;
	
	// define answer-ports
	
	// define request-handlers
	
	
	// define default slave ports
	SmartACE::StateSlave *stateSlave;
	StateActivityManager *stateActivityManager;
	SmartStateChangeHandler *stateChangeHandler;
	SmartACE::WiringSlave *wiringSlave;
	ParamUpdateHandler paramHandler;
	SmartACE::ParameterSlave *param;
	
	
	/// this method is used to register different PortFactory classes (one for each supported middleware framework)
	void addPortFactory(const std::string &name, ComponentWebInterfaceComissioningPortFactoryInterface *portFactory);
	
	SmartACE::SmartComponent* getComponentImpl();
	
	/// this method is used to register different component-extension classes
	void addExtension(ComponentWebInterfaceComissioningExtension *extension);
	
	/// this method allows to access the registered component-extensions (automatically converting to the actuall implementation type)
	template <typename T>
	T* getExtension(const std::string &name) {
		auto it = componentExtensionRegistry.find(name);
		if(it != componentExtensionRegistry.end()) {
			return dynamic_cast<T*>(it->second);
		}
		return 0;
	}
	
	/// initialize component's internal members
	void init(int argc, char *argv[]);
	
	/// execute the component's infrastructure
	void run();
	
	/// clean-up component's resources
	void fini();
	
	/// call this method to set the overall component into the Alive state (i.e. component is then ready to operate)
	void setStartupFinished();
	
	/// connect all component's client ports
	Smart::StatusCode connectAndStartAllServices();
	
	/// start all assocuated Activities
	void startAllTasks();
	
	/// start all associated timers
	void startAllTimers();
	

	// return singleton instance
	static ComponentWebInterfaceComissioning* instance()
	{
		if(_componentWebInterfaceComissioning == 0) {
			_componentWebInterfaceComissioning = new ComponentWebInterfaceComissioning();
		}
		return _componentWebInterfaceComissioning;
	}
	
	static void deleteInstance() {
		if(_componentWebInterfaceComissioning != 0) {
			delete _componentWebInterfaceComissioning;
		}
	}
	
	// connections parameter
	struct connections_struct
	{
		// component struct
		struct component_struct
		{
			// the name of the component
			std::string name;
			std::string initialComponentMode;
			std::string defaultScheduler;
			bool useLogger;
		} component;
		
		//--- task parameter ---
		struct DataTask_struct {
			double minActFreq;
			double maxActFreq;
			std::string trigger;
			// only one of the following two params is 
			// actually used at run-time according 
			// to the system config model
			double periodicActFreq;
			// or
			std::string inPortRef;
			int prescale;
			// scheduling parameters
			std::string scheduler;
			int priority;
			int cpuAffinity;
		} dataTask;
		struct WorkerTask_struct {
			double minActFreq;
			double maxActFreq;
			std::string trigger;
			// only one of the following two params is 
			// actually used at run-time according 
			// to the system config model
			double periodicActFreq;
			// or
			std::string inPortRef;
			int prescale;
			// scheduling parameters
			std::string scheduler;
			int priority;
			int cpuAffinity;
		} workerTask;
		
		//--- upcall parameter ---
		struct SendHandler_struct {
			int prescale;
		} sendHandler;
		
		//--- server port parameter ---
		struct SequencerEventServer_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} sequencerEventServer;
		struct SequencerSendServer_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} sequencerSendServer;
	
		//--- client port parameter ---
		
	} connections;
};
#endif
