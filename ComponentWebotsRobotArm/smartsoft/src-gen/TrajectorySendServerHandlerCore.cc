//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "TrajectorySendServerHandlerCore.hh"
#include "TrajectorySendServerHandler.hh"

TrajectorySendServerHandlerCore::TrajectorySendServerHandlerCore(
	Smart::InputSubject<CommManipulatorObjects::CommManipulatorTrajectory> *subject,
	const int &prescaleFactor)
	:	Smart::InputTaskTrigger<CommManipulatorObjects::CommManipulatorTrajectory>(subject, prescaleFactor)
{
}
TrajectorySendServerHandlerCore::~TrajectorySendServerHandlerCore()
{  
}


void TrajectorySendServerHandlerCore::updateAllCommObjects() {
}

void TrajectorySendServerHandlerCore::notify_all_interaction_observers() {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	// try dynamically down-casting this class to the derived class 
	// (we can do it safely here as we exactly know the derived class)
	if(const TrajectorySendServerHandler* trajectorySendServerHandler = dynamic_cast<const TrajectorySendServerHandler*>(this)) {
		for(auto it=interaction_observers.begin(); it!=interaction_observers.end(); it++) {
			(*it)->on_update_from(trajectorySendServerHandler);
		}
	}
}

void TrajectorySendServerHandlerCore::attach_interaction_observer(TrajectorySendServerHandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.push_back(observer);
}

void TrajectorySendServerHandlerCore::detach_interaction_observer(TrajectorySendServerHandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.remove(observer);
}
