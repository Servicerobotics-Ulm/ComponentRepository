//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _JOBSOURCE2EVENTCLIENT_UPCALL_MANAGER_HH
#define _JOBSOURCE2EVENTCLIENT_UPCALL_MANAGER_HH

#include <list>
#include "aceSmartSoft.hh"
#include "JobSource2EventClientUpcallInterface.hh"

/** JobSource2EventClientUpcallManager connects input-handling with Upcall propagation
 *
 * This class implements an InputHandler for the InputPort jobSource2EventClient and propagates the handling 
 * of incoming data to all associated (i.e. attached) Upcalls.
 */
class JobSource2EventClientUpcallManager
:	public Smart::IInputHandler<Smart::EventInputType<CommBasicObjects::CommTaskMessage>>
{
private:
	// list of associated updalls
	std::list<JobSource2EventClientUpcallInterface*> upcalls;

	// call the on_jobSource2EventClient of all the attached JobSource2EventClientUpcallInterfaces
	void notify_upcalls(const Smart::EventInputType<CommBasicObjects::CommTaskMessage> &input);
	
protected:
	virtual void handle_input(const Smart::EventInputType<CommBasicObjects::CommTaskMessage> &input) {
		// relay input-handling to all attached JobSource2EventClientUpcallInterfaces
		this->notify_upcalls(input);
	}
public:
	JobSource2EventClientUpcallManager(
		Smart::InputSubject<Smart::EventInputType<CommBasicObjects::CommTaskMessage>> *subject,
		const int &prescaleFactor=1
	);
	virtual ~JobSource2EventClientUpcallManager();
	
	void attach(JobSource2EventClientUpcallInterface *upcall);
	void detach(JobSource2EventClientUpcallInterface *upcall);
};

#endif
