//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "JobResultSendHandlerCore.hh"
#include "JobResultSendHandler.hh"

JobResultSendHandlerCore::JobResultSendHandlerCore(
	Smart::InputSubject<CommBasicObjects::CommTaskMessage> *subject,
	const int &prescaleFactor)
	:	Smart::InputTaskTrigger<CommBasicObjects::CommTaskMessage>(subject, prescaleFactor)
{
	signalled_to_stop = false;
	this->start();
}
JobResultSendHandlerCore::~JobResultSendHandlerCore()
{  
	this->stop();
}

// inputs are pushed to the request_queue
void JobResultSendHandlerCore::handle_input(const CommBasicObjects::CommTaskMessage &input) {
	std::unique_lock<std::mutex> scoped_lock(handler_mutex);
	request_queue.push_back(input);
	scoped_lock.unlock();
	
	handler_cond_var.notify_all();
}
// inputs are processed from within the thread, thus implementing an active FIFO request-queue
int JobResultSendHandlerCore::task_execution() {
	while(!signalled_to_stop) {
		std::unique_lock<std::mutex> scoped_lock(handler_mutex);
		if(request_queue.empty()) {
			handler_cond_var.wait(scoped_lock);
			if(signalled_to_stop)
				return 0;
		}
		CommBasicObjects::CommTaskMessage input = request_queue.front();
		request_queue.pop_front();
		scoped_lock.unlock();
		
		this->updateAllCommObjects();
		// call the input handler method (which has to be implemented in derived classes)
		this->on_jobResultSendServer(input);
		// notify all attached interaction observers
		this->notify_all_interaction_observers();
		// call implementation of base class
		Smart::InputTaskTrigger<CommBasicObjects::CommTaskMessage>::handle_input(input);
	}
	return 0;
}

void JobResultSendHandlerCore::updateAllCommObjects() {
}

void JobResultSendHandlerCore::notify_all_interaction_observers() {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	// try dynamically down-casting this class to the derived class 
	// (we can do it safely here as we exactly know the derived class)
	if(const JobResultSendHandler* jobResultSendHandler = dynamic_cast<const JobResultSendHandler*>(this)) {
		for(auto it=interaction_observers.begin(); it!=interaction_observers.end(); it++) {
			(*it)->on_update_from(jobResultSendHandler);
		}
	}
}

void JobResultSendHandlerCore::attach_interaction_observer(JobResultSendHandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.push_back(observer);
}

void JobResultSendHandlerCore::detach_interaction_observer(JobResultSendHandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.remove(observer);
}
