//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "JobSource2EventHandlerCore.hh"
#include "JobSource2EventHandler.hh"

JobSource2EventHandlerCore::JobSource2EventHandlerCore(
	Smart::InputSubject<Smart::EventInputType<CommBasicObjects::CommTaskMessage>> *subject,
	const int &prescaleFactor)
	:	Smart::InputTaskTrigger<Smart::EventInputType<CommBasicObjects::CommTaskMessage>>(subject, prescaleFactor)
{
}
JobSource2EventHandlerCore::~JobSource2EventHandlerCore()
{  
}


void JobSource2EventHandlerCore::updateAllCommObjects() {
}

void JobSource2EventHandlerCore::notify_all_interaction_observers() {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	// try dynamically down-casting this class to the derived class 
	// (we can do it safely here as we exactly know the derived class)
	if(const JobSource2EventHandler* jobSource2EventHandler = dynamic_cast<const JobSource2EventHandler*>(this)) {
		for(auto it=interaction_observers.begin(); it!=interaction_observers.end(); it++) {
			(*it)->on_update_from(jobSource2EventHandler);
		}
	}
}

void JobSource2EventHandlerCore::attach_interaction_observer(JobSource2EventHandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.push_back(observer);
}

void JobSource2EventHandlerCore::detach_interaction_observer(JobSource2EventHandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.remove(observer);
}
