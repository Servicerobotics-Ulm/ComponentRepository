//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------

#include "SmartGMappingAcePortFactory.hh"

// create a static instance of the default AcePortFactory
static SmartGMappingAcePortFactory acePortFactory;

SmartGMappingAcePortFactory::SmartGMappingAcePortFactory()
{  
	componentImpl = 0;
	SmartGMapping::instance()->addPortFactory("ACE_SmartSoft", this);
}

SmartGMappingAcePortFactory::~SmartGMappingAcePortFactory()
{  }

void SmartGMappingAcePortFactory::initialize(SmartGMapping *component, int argc, char* argv[])
{
	if(component->connections.component.defaultScheduler != "DEFAULT") {
		ACE_Sched_Params sched_params(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(component->connections.component.defaultScheduler == "FIFO") {
			sched_params.policy(ACE_SCHED_FIFO);
			sched_params.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(component->connections.component.defaultScheduler == "RR") {
			sched_params.policy(ACE_SCHED_RR);
			sched_params.priority(ACE_THR_PRI_RR_MIN);
		}
		// create new instance of the SmartSoft component with customized scheuling parameters 
		componentImpl = new SmartGMappingImpl(component->connections.component.name, argc, argv, sched_params);
	} else {
		// create new instance of the SmartSoft component
		componentImpl = new SmartGMappingImpl(component->connections.component.name, argc, argv);
	}
}

int SmartGMappingAcePortFactory::onStartup()
{
	return componentImpl->startComponentInfrastructure();
}

Smart::IPushClientPattern<CommBasicObjects::CommMobileLaserScan> * SmartGMappingAcePortFactory::createLaserServiceIn()
{
	return new SmartACE::PushClient<CommBasicObjects::CommMobileLaserScan>(componentImpl);
}

Smart::ISendClientPattern<CommBasicObjects::CommBasePositionUpdate> * SmartGMappingAcePortFactory::createLocalizationUpdateServiceOut()
{
	return new SmartACE::SendClient<CommBasicObjects::CommBasePositionUpdate>(componentImpl);
}


Smart::IPushServerPattern<CommNavigationObjects::CommGridMap> * SmartGMappingAcePortFactory::createCurrGridMapPushServiceOut(const std::string &serviceName)
{
	return new SmartACE::PushServer<CommNavigationObjects::CommGridMap>(componentImpl, serviceName);
}


SmartACE::SmartComponent* SmartGMappingAcePortFactory::getComponentImpl()
{
	return componentImpl;
}

int SmartGMappingAcePortFactory::onShutdown(const std::chrono::steady_clock::duration &timeoutTime)
{
	componentImpl->stopComponentInfrastructure(timeoutTime);
	return 0;
}

void SmartGMappingAcePortFactory::destroy()
{
	// clean-up component's internally used resources (internally used communication middleware) 
	componentImpl->cleanUpComponentResources();
	delete componentImpl;
}
