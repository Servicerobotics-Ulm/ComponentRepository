//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------

#include "TriggerHandlerCore.hh"

int TriggerHandlerCore::on_execute()
{
	// blocking wait until some active trigger request(s) come in
	sema.acquire();
	
	{
		SmartACE::SmartGuard g(mutex);
		
		// get the top trigger from the queue
		current_trigger_enumerator = trigger_queue.front();
		trigger_queue.pop();
		
		// retrieve the corresponding trigger attributes from the trigger specific queue
		switch(current_trigger_enumerator) {
		case TriggerEnumerators::COMMROBOTINOOBJECTS_ROBOTINODOCKINGPARAMETER_SAVE_BEFORE_DOCKING_POSE:
			current_CommRobotinoObjects_RobotinoDockingParameter_SAVE_BEFORE_DOCKING_POSE = CommRobotinoObjects_RobotinoDockingParameter_SAVE_BEFORE_DOCKING_POSE_queue.front();
			CommRobotinoObjects_RobotinoDockingParameter_SAVE_BEFORE_DOCKING_POSE_queue.pop();
			break;
		}
	} // mutex release

	// now call the corresponding trigger handler
	// (releasing the mutex before, allows to store newly incoming trigger commands on the queue in parallel)
	switch(current_trigger_enumerator) {
	case TriggerEnumerators::COMMROBOTINOOBJECTS_ROBOTINODOCKINGPARAMETER_SAVE_BEFORE_DOCKING_POSE:
		this->handleCommRobotinoObjects_RobotinoDockingParameter_SAVE_BEFORE_DOCKING_POSE();
		break;
	}
	return 0;
}

//
// trigger internal handler methods
//

	// handle SAVE_BEFORE_DOCKING_POSE
	void TriggerHandlerCore::handleCommRobotinoObjects_RobotinoDockingParameter_SAVE_BEFORE_DOCKING_POSECore()
	{
		SmartACE::SmartGuard g(mutex);

		// store the current set of trigger-attributes in a queue
		CommRobotinoObjects_RobotinoDockingParameter_SAVE_BEFORE_DOCKING_POSEAttributes attr;
		CommRobotinoObjects_RobotinoDockingParameter_SAVE_BEFORE_DOCKING_POSE_queue.push(attr);
		
		// store the current trigger call in the shared trigger-queue
		trigger_queue.push(TriggerEnumerators::COMMROBOTINOOBJECTS_ROBOTINODOCKINGPARAMETER_SAVE_BEFORE_DOCKING_POSE);
		
		// signal the task, in case it is waiting
		sema.release();
	}

//
// extended trigger internal handler methods
//
