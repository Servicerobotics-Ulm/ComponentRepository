//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _COMPONENTTRAFFICLIGHT_HH
#define _COMPONENTTRAFFICLIGHT_HH

#include <map>
#include <iostream>
#include "aceSmartSoft.hh"
#include "smartQueryServerTaskTrigger_T.h"
#include "ComponentTrafficLightCore.hh"

#include "ComponentTrafficLightPortFactoryInterface.hh"
#include "ComponentTrafficLightExtension.hh"

// forward declarations
class ComponentTrafficLightPortFactoryInterface;
class ComponentTrafficLightExtension;

// includes for ComponentTrafficLightROS1InterfacesExtension

// includes for ComponentTrafficLightROSExtension

// includes for ComponentTrafficLightRestInterfacesExtension

// includes for OpcUaBackendComponentGeneratorExtension

// includes for PlainOpcUaComponentTrafficLightExtension
// include plain OPC UA device clients
// include plain OPC UA status servers


// include communication objects
#include <DomainHMI/CommTrafficLight.hh>
#include <DomainHMI/CommTrafficLightACE.hh>

// include tasks
// include UpcallManagers and InputCollectors
#include "TrafficLightServiceInUpcallManager.hh"
#include "TrafficLightServiceInInputCollector.hh"

// include input-handler(s)
#include "TrafficLightServiceInHandler.hh"
// include request-handler(s)
// output port wrappers

// include handler
#include "CompHandler.hh"


#include "SmartStateChangeHandler.hh"

#define COMP ComponentTrafficLight::instance()

class ComponentTrafficLight : public ComponentTrafficLightCore {
private:
	static ComponentTrafficLight *_componentTrafficLight;
	
	// constructor
	ComponentTrafficLight();
	
	// copy-constructor
	ComponentTrafficLight(const ComponentTrafficLight& cc);
	
	// destructor
	~ComponentTrafficLight() { };
	
	// load parameter from ini file
	void loadParameter(int argc, char* argv[]);
	
	// instantiate comp-handler
	CompHandler compHandler;
	
	// helper method that maps a string-name to an according TaskTriggerSubject
	Smart::TaskTriggerSubject* getInputTaskTriggerFromString(const std::string &client);
	
	// internal map storing the different port-creation factories (that internally map to specific middleware implementations)
	std::map<std::string, ComponentTrafficLightPortFactoryInterface*> portFactoryRegistry;
	
	// internal map storing various extensions of this component class
	std::map<std::string, ComponentTrafficLightExtension*> componentExtensionRegistry;
	
public:
	
	// define tasks
	
	// define input-ports
	// InputPort TrafficLightServiceIn
	Smart::ISendServerPattern<DomainHMI::CommTrafficLight> *trafficLightServiceIn;
	Smart::InputTaskTrigger<DomainHMI::CommTrafficLight> *trafficLightServiceInInputTaskTrigger;
	TrafficLightServiceInUpcallManager *trafficLightServiceInUpcallManager;
	TrafficLightServiceInInputCollector *trafficLightServiceInInputCollector;
	
	// define request-ports
	
	// define input-handler
	TrafficLightServiceInHandler *trafficLightServiceInHandler;
	
	// define output-ports
	
	// define answer-ports
	
	// define request-handlers
	
	// definitions of ComponentTrafficLightROS1InterfacesExtension
	
	// definitions of ComponentTrafficLightROSExtension
	
	// definitions of ComponentTrafficLightRestInterfacesExtension
	
	// definitions of OpcUaBackendComponentGeneratorExtension
	
	// definitions of PlainOpcUaComponentTrafficLightExtension
	
	
	// define default slave ports
	SmartACE::StateSlave *stateSlave;
	SmartStateChangeHandler *stateChangeHandler;
	SmartACE::WiringSlave *wiringSlave;
	
	
	/// this method is used to register different PortFactory classes (one for each supported middleware framework)
	void addPortFactory(const std::string &name, ComponentTrafficLightPortFactoryInterface *portFactory);
	
	SmartACE::SmartComponent* getComponentImpl();
	
	/// this method is used to register different component-extension classes
	void addExtension(ComponentTrafficLightExtension *extension);
	
	/// this method allows to access the registered component-extensions (automatically converting to the actuall implementation type)
	template <typename T>
	T* getExtension(const std::string &name) {
		auto it = componentExtensionRegistry.find(name);
		if(it != componentExtensionRegistry.end()) {
			return dynamic_cast<T*>(it->second);
		}
		return 0;
	}
	
	/// initialize component's internal members
	void init(int argc, char *argv[]);
	
	/// execute the component's infrastructure
	void run();
	
	/// clean-up component's resources
	void fini();
	
	/// call this method to set the overall component into the Alive state (i.e. component is then ready to operate)
	void setStartupFinished();
	
	/// connect all component's client ports
	Smart::StatusCode connectAndStartAllServices();
	
	/// start all assocuated Activities
	void startAllTasks();
	
	/// start all associated timers
	void startAllTimers();
	

	// return singleton instance
	static ComponentTrafficLight* instance()
	{
		if(_componentTrafficLight == 0) {
			_componentTrafficLight = new ComponentTrafficLight();
		}
		return _componentTrafficLight;
	}
	
	static void deleteInstance() {
		if(_componentTrafficLight != 0) {
			delete _componentTrafficLight;
		}
	}
	
	// connections parameter
	struct connections_struct
	{
		// component struct
		struct component_struct
		{
			// the name of the component
			std::string name;
			std::string initialComponentMode;
			std::string defaultScheduler;
			bool useLogger;
		} component;
		
		//--- task parameter ---
		
		//--- upcall parameter ---
		struct TrafficLightServiceInHandler_struct {
			int prescale;
		} trafficLightServiceInHandler;
		
		//--- server port parameter ---
		struct TrafficLightServiceIn_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} trafficLightServiceIn;
	
		//--- client port parameter ---
		
		// -- parameters for ComponentTrafficLightROS1InterfacesExtension
		
		// -- parameters for ComponentTrafficLightROSExtension
		
		// -- parameters for ComponentTrafficLightRestInterfacesExtension
		
		// -- parameters for OpcUaBackendComponentGeneratorExtension
		
		// -- parameters for PlainOpcUaComponentTrafficLightExtension
		
	} connections;
};
#endif
