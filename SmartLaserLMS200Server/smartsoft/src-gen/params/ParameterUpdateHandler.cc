//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ParameterUpdateHandler.hh"

#include "SmartLaserLMS200Server.hh"

SmartACE::CommParameterResponse ParamUpdateHandler::handleParameter(const SmartACE::CommParameterRequest& request)
{
	SmartACE::CommParameterResponse answer;

	std::string tag = request.getTag();
	std::cout<<"PARAMETER: "<<tag<<std::endl;
	
	if (tag == "COMMIT")
	{
		answer.setResponse(globalState.handleCOMMIT(commitState));
		if(answer.getResponse() == SmartACE::ParamResponseType::OK) {
			globalStateLock.acquire();
			// change the content of the globalState, however change only the generated content
			// without affecting potential user member variables (which is more intuitive for the user)
			globalState.setContent(commitState);
			globalStateLock.release();
		} else {
			// the commit validation check returned != OK
			// the commit state is rejected and is not copied into the global state
		}
	}
	else
	{
		/////////////////////////////////////////////////////////////////////
		// default new
		std::cout<<"ERROR wrong Parameter!"<<std::endl;
		answer.setResponse(SmartACE::ParamResponseType::INVALID);
	}
	

	std::cout<<"[handleQuery] PARAMETER "<<tag<<" DONE\n\n";

	return answer;
}


ParameterStateStruct ParamUpdateHandler::getGlobalState() const{
	SmartACE::SmartGuard g(globalStateLock);
	return this->globalState;
}


void ParamUpdateHandler::loadParameter(SmartACE::SmartIniParameter &parameter)
{
	/*
	 Parameters can be specified via command line -filename=<filename>

	 With this parameter present:
	 - The component will look for the file in the current working directory,
	 a path relative to the current directory or any absolute path
	 - The component will use the default values if the file cannot be found

	 With this parameter absent:
	 - <Name of Component>.ini will be read from current working directory, if found there
	 - $SMART_ROOT/etc/<Name of Component>.ini will be read otherwise
	 - Default values will be used if neither found in working directory or /etc
	 */

	// load parameters
	try
	{
		// print all known parameters
		parameter.print();

		//
		// load internal parameters (if any)
		//
		// parameter Base
		if(parameter.getDouble("Base", "base_a", commitState.Base.base_a))
		{
			globalState.Base.base_a = commitState.Base.base_a;
		}
		if(parameter.getBoolean("Base", "subscribe", commitState.Base.subscribe))
		{
			globalState.Base.subscribe = commitState.Base.subscribe;
		}
		if(parameter.getDouble("Base", "x", commitState.Base.x))
		{
			globalState.Base.x = commitState.Base.x;
		}
		if(parameter.getDouble("Base", "y", commitState.Base.y))
		{
			globalState.Base.y = commitState.Base.y;
		}
		if(parameter.getDouble("Base", "z", commitState.Base.z))
		{
			globalState.Base.z = commitState.Base.z;
		}
		// parameter Laser
		if(parameter.getDouble("Laser", "azimuth", commitState.Laser.azimuth))
		{
			globalState.Laser.azimuth = commitState.Laser.azimuth;
		}
		if(parameter.getInteger("Laser", "baudrate", commitState.Laser.baudrate))
		{
			globalState.Laser.baudrate = commitState.Laser.baudrate;
		}
		if(parameter.getString("Laser", "device", commitState.Laser.device))
		{
			globalState.Laser.device = commitState.Laser.device;
		}
		if(parameter.getInteger("Laser", "length_unit", commitState.Laser.length_unit))
		{
			globalState.Laser.length_unit = commitState.Laser.length_unit;
		}
		if(parameter.getInteger("Laser", "resolution", commitState.Laser.resolution))
		{
			globalState.Laser.resolution = commitState.Laser.resolution;
		}
		if(parameter.getString("Laser", "sick_type", commitState.Laser.sick_type))
		{
			globalState.Laser.sick_type = commitState.Laser.sick_type;
		}
		if(parameter.getBoolean("Laser", "verbose", commitState.Laser.verbose))
		{
			globalState.Laser.verbose = commitState.Laser.verbose;
		}
		if(parameter.getDouble("Laser", "x", commitState.Laser.x))
		{
			globalState.Laser.x = commitState.Laser.x;
		}
		if(parameter.getDouble("Laser", "y", commitState.Laser.y))
		{
			globalState.Laser.y = commitState.Laser.y;
		}
		if(parameter.getDouble("Laser", "z", commitState.Laser.z))
		{
			globalState.Laser.z = commitState.Laser.z;
		}
		
		//
		// load extended parameters (if any)
		//
		
		//
		// load instance parameters (if a parameter definition was instantiated in the model)
		//

	} catch (const SmartACE::IniParameterError & e)
	{
		std::cerr << "Exception from parameter handling: " << e << std::endl;
	} catch (const std::exception &ex)
	{
		std::cerr << "Uncaught std:: exception" << ex.what() << std::endl;
	} catch (...)
	{
		std::cerr << "Uncaught exception" << std::endl;
	}
}
