//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _COMPONENTUNICAPIMAGESERVER_HH
#define _COMPONENTUNICAPIMAGESERVER_HH

#include <map>
#include <iostream>
#include "aceSmartSoft.hh"
#include "smartQueryServerTaskTrigger_T.h"
#include "ComponentUnicapImageServerCore.hh"

#include "ComponentUnicapImageServerPortFactoryInterface.hh"
#include "ComponentUnicapImageServerExtension.hh"

// forward declarations
class ComponentUnicapImageServerPortFactoryInterface;
class ComponentUnicapImageServerExtension;

// includes for PlainOpcUaComponentUnicapImageServerExtension
// include plain OPC UA device clients
// include plain OPC UA status servers


// include communication objects
#include <CommBasicObjects/CommBaseState.hh>
#include <CommBasicObjects/CommBaseStateACE.hh>
#include <CommBasicObjects/CommDevicePoseState.hh>
#include <CommBasicObjects/CommDevicePoseStateACE.hh>
#include <DomainVision/CommVideoImage.hh>
#include <DomainVision/CommVideoImageACE.hh>
#include <CommBasicObjects/CommVoid.hh>
#include <CommBasicObjects/CommVoidACE.hh>

// include tasks
#include "ImageTask.hh"
// include UpcallManagers and InputCollectors
#include "BasePushTimedClientUpcallManager.hh"
#include "BasePushTimedClientInputCollector.hh"
#include "PtuPushTimedClientUpcallManager.hh"
#include "PtuPushTimedClientInputCollector.hh"

// include input-handler(s)
// include request-handler(s)
#include "ImageQueryHandler.hh"
// output port wrappers
#include "ImagePushNewestServerWrapper.hh"

// include handler
#include "CompHandler.hh"

#include "ParameterStateStruct.hh"
#include "ParameterUpdateHandler.hh"

#include "SmartStateChangeHandler.hh"
#include "StateActivityManager.hh"


#define COMP ComponentUnicapImageServer::instance()

class ComponentUnicapImageServer : public ComponentUnicapImageServerCore {
private:
	static ComponentUnicapImageServer *_componentUnicapImageServer;
	
	// constructor
	ComponentUnicapImageServer();
	
	// copy-constructor
	ComponentUnicapImageServer(const ComponentUnicapImageServer& cc);
	
	// destructor
	~ComponentUnicapImageServer() { };
	
	// load parameter from ini file
	void loadParameter(int argc, char* argv[]);
	
	// instantiate comp-handler
	CompHandler compHandler;
	
	// helper method that maps a string-name to an according TaskTriggerSubject
	Smart::TaskTriggerSubject* getInputTaskTriggerFromString(const std::string &client);
	
	// internal map storing the different port-creation factories (that internally map to specific middleware implementations)
	std::map<std::string, ComponentUnicapImageServerPortFactoryInterface*> portFactoryRegistry;
	
	// internal map storing various extensions of this component class
	std::map<std::string, ComponentUnicapImageServerExtension*> componentExtensionRegistry;
	
public:
	ParameterStateStruct getGlobalState() const
	{
		return paramHandler.getGlobalState();
	}
	
	ParameterStateStruct getParameters() const
	{
		return paramHandler.getGlobalState();
	}
	
	// define tasks
	Smart::TaskTriggerSubject* imageTaskTrigger;
	ImageTask *imageTask;
	
	// define input-ports
	// InputPort basePushTimedClient
	Smart::IPushClientPattern<CommBasicObjects::CommBaseState> *basePushTimedClient;
	Smart::InputTaskTrigger<CommBasicObjects::CommBaseState> *basePushTimedClientInputTaskTrigger;
	BasePushTimedClientUpcallManager *basePushTimedClientUpcallManager;
	BasePushTimedClientInputCollector *basePushTimedClientInputCollector;
	// InputPort ptuPushTimedClient
	Smart::IPushClientPattern<CommBasicObjects::CommDevicePoseState> *ptuPushTimedClient;
	Smart::InputTaskTrigger<CommBasicObjects::CommDevicePoseState> *ptuPushTimedClientInputTaskTrigger;
	PtuPushTimedClientUpcallManager *ptuPushTimedClientUpcallManager;
	PtuPushTimedClientInputCollector *ptuPushTimedClientInputCollector;
	
	// define request-ports
	
	// define input-handler
	
	// define output-ports
	Smart::IPushServerPattern<DomainVision::CommVideoImage> *imagePushNewestServer;
	ImagePushNewestServerWrapper *imagePushNewestServerWrapper;
	
	// define answer-ports
	Smart::IQueryServerPattern<CommBasicObjects::CommVoid, DomainVision::CommVideoImage> *imageQueryServer;
	Smart::QueryServerTaskTrigger<CommBasicObjects::CommVoid, DomainVision::CommVideoImage> *imageQueryServerInputTaskTrigger;
	
	// define request-handlers
	ImageQueryHandler *imageQueryHandler;
	
	
	// define default slave ports
	SmartACE::StateSlave *stateSlave;
	StateActivityManager *stateActivityManager;
	SmartStateChangeHandler *stateChangeHandler;
	SmartACE::WiringSlave *wiringSlave;
	ParamUpdateHandler paramHandler;
	SmartACE::ParameterSlave *param;
	
	
	/// this method is used to register different PortFactory classes (one for each supported middleware framework)
	void addPortFactory(const std::string &name, ComponentUnicapImageServerPortFactoryInterface *portFactory);
	
	SmartACE::SmartComponent* getComponentImpl();
	
	/// this method is used to register different component-extension classes
	void addExtension(ComponentUnicapImageServerExtension *extension);
	
	/// this method allows to access the registered component-extensions (automatically converting to the actuall implementation type)
	template <typename T>
	T* getExtension(const std::string &name) {
		auto it = componentExtensionRegistry.find(name);
		if(it != componentExtensionRegistry.end()) {
			return dynamic_cast<T*>(it->second);
		}
		return 0;
	}
	
	/// initialize component's internal members
	void init(int argc, char *argv[]);
	
	/// execute the component's infrastructure
	void run();
	
	/// clean-up component's resources
	void fini();
	
	/// call this method to set the overall component into the Alive state (i.e. component is then ready to operate)
	void setStartupFinished();
	
	/// connect all component's client ports
	Smart::StatusCode connectAndStartAllServices();
	
	/// start all assocuated Activities
	void startAllTasks();
	
	/// start all associated timers
	void startAllTimers();
	
	Smart::StatusCode connectBasePushTimedClient(const std::string &serverName, const std::string &serviceName);
	Smart::StatusCode connectPtuPushTimedClient(const std::string &serverName, const std::string &serviceName);

	// return singleton instance
	static ComponentUnicapImageServer* instance()
	{
		if(_componentUnicapImageServer == 0) {
			_componentUnicapImageServer = new ComponentUnicapImageServer();
		}
		return _componentUnicapImageServer;
	}
	
	static void deleteInstance() {
		if(_componentUnicapImageServer != 0) {
			delete _componentUnicapImageServer;
		}
	}
	
	// connections parameter
	struct connections_struct
	{
		// component struct
		struct component_struct
		{
			// the name of the component
			std::string name;
			std::string initialComponentMode;
			std::string defaultScheduler;
			bool useLogger;
		} component;
		
		//--- task parameter ---
		struct ImageTask_struct {
			double minActFreq;
			double maxActFreq;
			std::string trigger;
			// only one of the following two params is 
			// actually used at run-time according 
			// to the system config model
			double periodicActFreq;
			// or
			std::string inPortRef;
			int prescale;
			// scheduling parameters
			std::string scheduler;
			int priority;
			int cpuAffinity;
		} imageTask;
		
		//--- upcall parameter ---
		
		//--- server port parameter ---
		struct ImagePushNewestServer_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} imagePushNewestServer;
		struct ImageQueryServer_struct {
				std::string serviceName;
				std::string roboticMiddleware;
		} imageQueryServer;
	
		//--- client port parameter ---
		struct BasePushTimedClient_struct {
			bool initialConnect;
			std::string serverName;
			std::string serviceName;
			std::string wiringName;
			long interval;
			std::string roboticMiddleware;
		} basePushTimedClient;
		struct PtuPushTimedClient_struct {
			bool initialConnect;
			std::string serverName;
			std::string serviceName;
			std::string wiringName;
			long interval;
			std::string roboticMiddleware;
		} ptuPushTimedClient;
		
	} connections;
};
#endif
