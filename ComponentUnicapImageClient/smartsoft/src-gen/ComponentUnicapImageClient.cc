//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ComponentUnicapImageClient.hh"
#include "smartTimedTaskTrigger.h"
//FIXME: implement logging
//#include "smartGlobalLogger.hh"

// the ace port-factory is used as a default port-mapping
#include "ComponentUnicapImageClientAcePortFactory.hh"


// initialize static singleton pointer to zero
ComponentUnicapImageClient* ComponentUnicapImageClient::_componentUnicapImageClient = 0;

// constructor
ComponentUnicapImageClient::ComponentUnicapImageClient()
{
	std::cout << "constructor of ComponentUnicapImageClient\n";
	
	// set all pointer members to NULL
	//coordinationPort = NULL;
	//coordinationPort = NULL;
	guiTask = NULL;
	guiTaskTrigger = NULL;
	imageTask = NULL;
	imageTaskTrigger = NULL;
	pushNewestClient = NULL;
	pushNewestClientInputTaskTrigger = NULL;
	pushNewestClientUpcallManager = NULL;
	pushNewestClientInputCollector = NULL;
	queryClient = NULL;
	stateChangeHandler = NULL;
	stateSlave = NULL;
	wiringSlave = NULL;
	param = NULL;
	
	// set default ini parameter values
	connections.component.name = "ComponentUnicapImageClient";
	connections.component.initialComponentMode = "Neutral";
	connections.component.defaultScheduler = "DEFAULT";
	connections.component.useLogger = false;
	
	connections.pushNewestClient.initialConnect = false;
	connections.pushNewestClient.wiringName = "pushNewestClient";
	connections.pushNewestClient.serverName = "unknown";
	connections.pushNewestClient.serviceName = "unknown";
	connections.pushNewestClient.interval = 1;
	connections.pushNewestClient.roboticMiddleware = "ACE_SmartSoft";
	connections.queryClient.initialConnect = false;
	connections.queryClient.wiringName = "queryClient";
	connections.queryClient.serverName = "unknown";
	connections.queryClient.serviceName = "unknown";
	connections.queryClient.interval = 1;
	connections.queryClient.roboticMiddleware = "ACE_SmartSoft";
	connections.guiTask.minActFreq = 0.0;
	connections.guiTask.maxActFreq = 0.0;
	connections.guiTask.trigger = "PeriodicTimer";
	connections.guiTask.periodicActFreq = 30.0;
	// scheduling default parameters
	connections.guiTask.scheduler = "DEFAULT";
	connections.guiTask.priority = -1;
	connections.guiTask.cpuAffinity = -1;
	connections.imageTask.minActFreq = 0.0;
	connections.imageTask.maxActFreq = 0.0;
	connections.imageTask.trigger = "PeriodicTimer";
	connections.imageTask.periodicActFreq = 30.0;
	// scheduling default parameters
	connections.imageTask.scheduler = "DEFAULT";
	connections.imageTask.priority = -1;
	connections.imageTask.cpuAffinity = -1;
	
	// initialize members of ComponentUnicapImageClientROS1InterfacesExtension
	
	// initialize members of ComponentUnicapImageClientROSExtension
	
	// initialize members of ComponentUnicapImageClientRestInterfacesExtension
	
	// initialize members of OpcUaBackendComponentGeneratorExtension
	
	// initialize members of PlainOpcUaComponentUnicapImageClientExtension
	
}

void ComponentUnicapImageClient::addPortFactory(const std::string &name, ComponentUnicapImageClientPortFactoryInterface *portFactory)
{
	portFactoryRegistry[name] = portFactory;
}

void ComponentUnicapImageClient::addExtension(ComponentUnicapImageClientExtension *extension)
{
	componentExtensionRegistry[extension->getName()] = extension;
}

SmartACE::SmartComponent* ComponentUnicapImageClient::getComponentImpl()
{
	return dynamic_cast<ComponentUnicapImageClientAcePortFactory*>(portFactoryRegistry["ACE_SmartSoft"])->getComponentImpl();
}

/**
 * Notify the component that setup/initialization is finished.
 * You may call this function from anywhere in the component.
 *
 * Set component's internal lifecycle state automaton (if any) into 
 * Alive mode (from here on the component is ready to provide its services)
 */
void ComponentUnicapImageClient::setStartupFinished() {
	stateSlave->setWaitState("Alive");
	std::cout << "ComponentDefinition initialization/startup finished." << std::endl;
}


Smart::StatusCode ComponentUnicapImageClient::connectPushNewestClient(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	if(connections.pushNewestClient.initialConnect == false) {
		return Smart::SMART_OK;
	}
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = pushNewestClient->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->pushNewestClient->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	pushNewestClient->subscribe(connections.pushNewestClient.interval);
	return status;
}
Smart::StatusCode ComponentUnicapImageClient::connectQueryClient(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	if(connections.queryClient.initialConnect == false) {
		return Smart::SMART_OK;
	}
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = queryClient->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->queryClient->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	return status;
}


/**
 * First connect ALL client ports contained in this component, then start all services:
 * activate state, push, etc...
 */
Smart::StatusCode ComponentUnicapImageClient::connectAndStartAllServices() {
	Smart::StatusCode status = Smart::SMART_OK;
	
	status = connectPushNewestClient(connections.pushNewestClient.serverName, connections.pushNewestClient.serviceName);
	if(status != Smart::SMART_OK) return status;
	status = connectQueryClient(connections.queryClient.serverName, connections.queryClient.serviceName);
	if(status != Smart::SMART_OK) return status;
	return status;
}

/**
 * Start all tasks contained in this component.
 */
void ComponentUnicapImageClient::startAllTasks() {
	// start task GuiTask
	if(connections.guiTask.scheduler != "DEFAULT") {
		ACE_Sched_Params guiTask_SchedParams(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(connections.guiTask.scheduler == "FIFO") {
			guiTask_SchedParams.policy(ACE_SCHED_FIFO);
			guiTask_SchedParams.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(connections.guiTask.scheduler == "RR") {
			guiTask_SchedParams.policy(ACE_SCHED_RR);
			guiTask_SchedParams.priority(ACE_THR_PRI_RR_MIN);
		}
		guiTask->start(guiTask_SchedParams, connections.guiTask.cpuAffinity);
	} else {
		guiTask->start();
	}
	// start task ImageTask
	if(connections.imageTask.scheduler != "DEFAULT") {
		ACE_Sched_Params imageTask_SchedParams(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(connections.imageTask.scheduler == "FIFO") {
			imageTask_SchedParams.policy(ACE_SCHED_FIFO);
			imageTask_SchedParams.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(connections.imageTask.scheduler == "RR") {
			imageTask_SchedParams.policy(ACE_SCHED_RR);
			imageTask_SchedParams.priority(ACE_THR_PRI_RR_MIN);
		}
		imageTask->start(imageTask_SchedParams, connections.imageTask.cpuAffinity);
	} else {
		imageTask->start();
	}
}

/**
 * Start all timers contained in this component
 */
void ComponentUnicapImageClient::startAllTimers() {
}


Smart::TaskTriggerSubject* ComponentUnicapImageClient::getInputTaskTriggerFromString(const std::string &client)
{
	if(client == "pushNewestClient") return pushNewestClientInputTaskTrigger;
	
	return NULL;
}


void ComponentUnicapImageClient::init(int argc, char *argv[])
{
	try {
		Smart::StatusCode status;
		
		// load initial parameters from ini-file (if found)
		loadParameter(argc, argv);
		
		// print out the actual parameters which are used to initialize the component
		std::cout << " \nComponentDefinition Initial-Parameters:\n" << COMP->getParameters() << std::endl;
		
		// initializations of ComponentUnicapImageClientROS1InterfacesExtension
		
		// initializations of ComponentUnicapImageClientROSExtension
		
		// initializations of ComponentUnicapImageClientRestInterfacesExtension
		
		// initializations of OpcUaBackendComponentGeneratorExtension
		
		// initializations of PlainOpcUaComponentUnicapImageClientExtension
		
		
		// initialize all registered port-factories
		for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
		{
			portFactory->second->initialize(this, argc, argv);
		}
		
		// initialize all registered component-extensions
		for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
		{
			extension->second->initialize(this, argc, argv);
		}
		
		ComponentUnicapImageClientPortFactoryInterface *acePortFactory = portFactoryRegistry["ACE_SmartSoft"];
		if(acePortFactory == 0) {
			std::cerr << "ERROR: acePortFactory NOT instantiated -> exit(-1)" << std::endl;
			exit(-1);
		}
		
		// this pointer is used for backwards compatibility (deprecated: should be removed as soon as all patterns, including coordination, are moved to port-factory)
		SmartACE::SmartComponent *component = dynamic_cast<ComponentUnicapImageClientAcePortFactory*>(acePortFactory)->getComponentImpl();
		
		std::cout << "ComponentDefinition ComponentUnicapImageClient is named " << connections.component.name << std::endl;
		
		if(connections.component.useLogger == true) {
			//FIXME: use logging
			//Smart::LOGGER->openLogFileInFolder("data/"+connections.component.name);
			//Smart::LOGGER->startLogging();
		}

		// create event-test handlers (if needed)
		
		// create server ports
		// TODO: set minCycleTime from Ini-file
		
		// create client ports
		pushNewestClient = portFactoryRegistry[connections.pushNewestClient.roboticMiddleware]->createPushNewestClient();
		queryClient = portFactoryRegistry[connections.queryClient.roboticMiddleware]->createQueryClient();
		
		// create InputTaskTriggers and UpcallManagers
		pushNewestClientInputCollector = new PushNewestClientInputCollector(pushNewestClient);
		pushNewestClientInputTaskTrigger = new Smart::InputTaskTrigger<DomainVision::CommVideoImage>(pushNewestClientInputCollector);
		pushNewestClientUpcallManager = new PushNewestClientUpcallManager(pushNewestClientInputCollector);
		
		// create input-handler
		
		// create request-handlers
		
		// create state pattern
		stateChangeHandler = new SmartStateChangeHandler();
		stateSlave = new SmartACE::StateSlave(component, stateChangeHandler);
		status = stateSlave->setUpInitialState(connections.component.initialComponentMode);
		if (status != Smart::SMART_OK) std::cerr << status << "; failed setting initial ComponentMode: " << connections.component.initialComponentMode << std::endl;
		// activate state slave
		status = stateSlave->activate();
		if(status != Smart::SMART_OK) std::cerr << "ERROR: activate state" << std::endl;
		
		wiringSlave = new SmartACE::WiringSlave(component);
		// add client port to wiring slave
		if(connections.pushNewestClient.roboticMiddleware == "ACE_SmartSoft") {
			//FIXME: this must also work with other implementations
			dynamic_cast<SmartACE::PushClient<DomainVision::CommVideoImage>*>(pushNewestClient)->add(wiringSlave, connections.pushNewestClient.wiringName);
		}
		if(connections.queryClient.roboticMiddleware == "ACE_SmartSoft") {
			//FIXME: this must also work with other implementations
			dynamic_cast<SmartACE::QueryClient<CommBasicObjects::CommVoid, DomainVision::CommVideoImage>*>(queryClient)->add(wiringSlave, connections.queryClient.wiringName);
		}
		
		// create parameter slave
		param = new SmartACE::ParameterSlave(component, &paramHandler);
		
		
		// create Task GuiTask
		guiTask = new GuiTask(component);
		// configure input-links
		// configure task-trigger (if task is configurable)
		if(connections.guiTask.trigger == "PeriodicTimer") {
			// create PeriodicTimerTrigger
			int microseconds = 1000*1000 / connections.guiTask.periodicActFreq;
			if(microseconds > 0) {
				Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
				triggerPtr->attach(guiTask);
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				// store trigger in class member
				guiTaskTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task GuiTask" << std::endl;
			}
		} else if(connections.guiTask.trigger == "DataTriggered") {
			guiTaskTrigger = getInputTaskTriggerFromString(connections.guiTask.inPortRef);
			if(guiTaskTrigger != NULL) {
				guiTaskTrigger->attach(guiTask, connections.guiTask.prescale);
			} else {
				std::cerr << "ERROR: could not set-up InPort " << connections.guiTask.inPortRef << " as activation source for Task GuiTask" << std::endl;
			}
		} else
		{
			// setup default task-trigger as PeriodicTimer
			Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
			int microseconds = 1000*1000 / 30.0;
			if(microseconds > 0) {
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				triggerPtr->attach(guiTask);
				// store trigger in class member
				guiTaskTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task GuiTask" << std::endl;
			}
		}
		
		// create Task ImageTask
		imageTask = new ImageTask(component);
		// configure input-links
		// configure task-trigger (if task is configurable)
		if(connections.imageTask.trigger == "PeriodicTimer") {
			// create PeriodicTimerTrigger
			int microseconds = 1000*1000 / connections.imageTask.periodicActFreq;
			if(microseconds > 0) {
				Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
				triggerPtr->attach(imageTask);
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				// store trigger in class member
				imageTaskTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task ImageTask" << std::endl;
			}
		} else if(connections.imageTask.trigger == "DataTriggered") {
			imageTaskTrigger = getInputTaskTriggerFromString(connections.imageTask.inPortRef);
			if(imageTaskTrigger != NULL) {
				imageTaskTrigger->attach(imageTask, connections.imageTask.prescale);
			} else {
				std::cerr << "ERROR: could not set-up InPort " << connections.imageTask.inPortRef << " as activation source for Task ImageTask" << std::endl;
			}
		} else
		{
			// setup default task-trigger as PeriodicTimer
			Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
			int microseconds = 1000*1000 / 30.0;
			if(microseconds > 0) {
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				triggerPtr->attach(imageTask);
				// store trigger in class member
				imageTaskTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task ImageTask" << std::endl;
			}
		}
		
		
		// link observers with subjects
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std exception" << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}

// run the component
void ComponentUnicapImageClient::run()
{
	stateSlave->acquire("init");
	// startup all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->onStartup();
	}
	
	// startup all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->onStartup();
	}
	stateSlave->release("init");
	
	// do not call this handler within the init state (see above) as this handler internally calls setStartupFinished() (this should be fixed in future)
	compHandler.onStartup();
	
	// this call blocks until the component is commanded to shutdown
	stateSlave->acquire("shutdown");
	
	// shutdown all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->onShutdown();
	}
	
	// shutdown all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->onShutdown();
	}
	
	if(connections.component.useLogger == true) {
		//FIXME: use logging
		//Smart::LOGGER->stopLogging();
	}
	
	compHandler.onShutdown();
	
	stateSlave->release("shutdown");
}

// clean-up component's resources
void ComponentUnicapImageClient::fini()
{
	// unlink all observers
	
	// destroy all task instances
	// unlink all UpcallManagers
	// unlink the TaskTrigger
	if(guiTaskTrigger != NULL){
		guiTaskTrigger->detach(guiTask);
		delete guiTask;
	}
	// unlink all UpcallManagers
	// unlink the TaskTrigger
	if(imageTaskTrigger != NULL){
		imageTaskTrigger->detach(imageTask);
		delete imageTask;
	}

	// destroy all input-handler

	// destroy InputTaskTriggers and UpcallManagers
	delete pushNewestClientInputTaskTrigger;
	delete pushNewestClientUpcallManager;
	delete pushNewestClientInputCollector;

	// destroy client ports
	delete pushNewestClient;
	delete queryClient;

	// destroy server ports
	// destroy event-test handlers (if needed)
	
	// destroy request-handlers
	
	delete stateSlave;
	// destroy state-change-handler
	delete stateChangeHandler;
	
	// destroy all master/slave ports
	delete wiringSlave;
	delete param;
	

	// destroy all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->destroy();
	}

	// destroy all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->destroy();
	}
	
	// destruction of ComponentUnicapImageClientROS1InterfacesExtension
	
	// destruction of ComponentUnicapImageClientROSExtension
	
	// destruction of ComponentUnicapImageClientRestInterfacesExtension
	
	// destruction of OpcUaBackendComponentGeneratorExtension
	
	// destruction of PlainOpcUaComponentUnicapImageClientExtension
	
}

void ComponentUnicapImageClient::loadParameter(int argc, char *argv[])
{
	/*
	 Parameters can be specified via command line --filename=<filename> or -f <filename>

	 With this parameter present:
	   - The component will look for the file in the current working directory,
	     a path relative to the current directory or any absolute path
	   - The component will use the default values if the file cannot be found

	 With this parameter absent:
	   - <Name of Component>.ini will be read from current working directory, if found there
	   - $SMART_ROOT/etc/<Name of Component>.ini will be read otherwise
	   - Default values will be used if neither found in working directory or /etc
	 */
	SmartACE::SmartIniParameter parameter;
	std::ifstream parameterfile;
	bool parameterFileFound = false;

	// load parameters
	try
	{
		// if paramfile is given as argument
		if(parameter.tryAddFileFromArgs(argc,argv,"filename", 'f'))
		{
			parameterFileFound = true;
			std::cout << "parameter file is loaded from an argv argument \n";
		} else if(parameter.searchFile("ComponentUnicapImageClient.ini", parameterfile)) {
			parameterFileFound = true;
			std::cout << "load ComponentUnicapImageClient.ini parameter file\n";
			parameter.addFile(parameterfile);
		} else {
			std::cout << "WARNING: ComponentUnicapImageClient.ini parameter file not found! (using default values or command line arguments)\n";
		}
		
		// add command line arguments to allow overwriting of parameters
		// from file
		parameter.addCommandLineArgs(argc,argv,"component");
		
		// initialize the naming service using the command line parameters parsed in the
		// SmartIniParameter class. The naming service parameters are expected to be in
		// the "component" parameter group.
		SmartACE::NAMING::instance()->checkForHelpArg(argc,argv);
		if(parameterFileFound) 
		{
			if(SmartACE::NAMING::instance()->init(parameter.getAllParametersFromGroup("component")) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		} else {
			if(SmartACE::NAMING::instance()->init(argc, argv) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		}
			
		// print all known parameters
		// parameter.print();
		
		//--- server port // client port // other parameter ---
		// load parameter
		parameter.getString("component", "name", connections.component.name);
		parameter.getString("component", "initialComponentMode", connections.component.initialComponentMode);
		if(parameter.checkIfParameterExists("component", "defaultScheduler")) {
			parameter.getString("component", "defaultScheduler", connections.component.defaultScheduler);
		}
		if(parameter.checkIfParameterExists("component", "useLogger")) {
			parameter.getBoolean("component", "useLogger", connections.component.useLogger);
		}
		
		// load parameters for client pushNewestClient
		parameter.getBoolean("pushNewestClient", "initialConnect", connections.pushNewestClient.initialConnect);
		parameter.getString("pushNewestClient", "serviceName", connections.pushNewestClient.serviceName);
		parameter.getString("pushNewestClient", "serverName", connections.pushNewestClient.serverName);
		parameter.getString("pushNewestClient", "wiringName", connections.pushNewestClient.wiringName);
		parameter.getInteger("pushNewestClient", "interval", connections.pushNewestClient.interval);
		if(parameter.checkIfParameterExists("pushNewestClient", "roboticMiddleware")) {
			parameter.getString("pushNewestClient", "roboticMiddleware", connections.pushNewestClient.roboticMiddleware);
		}
		// load parameters for client queryClient
		parameter.getBoolean("queryClient", "initialConnect", connections.queryClient.initialConnect);
		parameter.getString("queryClient", "serviceName", connections.queryClient.serviceName);
		parameter.getString("queryClient", "serverName", connections.queryClient.serverName);
		parameter.getString("queryClient", "wiringName", connections.queryClient.wiringName);
		if(parameter.checkIfParameterExists("queryClient", "roboticMiddleware")) {
			parameter.getString("queryClient", "roboticMiddleware", connections.queryClient.roboticMiddleware);
		}
		
		
		// load parameters for task GuiTask
		parameter.getDouble("GuiTask", "minActFreqHz", connections.guiTask.minActFreq);
		parameter.getDouble("GuiTask", "maxActFreqHz", connections.guiTask.maxActFreq);
		parameter.getString("GuiTask", "triggerType", connections.guiTask.trigger);
		if(connections.guiTask.trigger == "PeriodicTimer") {
			parameter.getDouble("GuiTask", "periodicActFreqHz", connections.guiTask.periodicActFreq);
		} else if(connections.guiTask.trigger == "DataTriggered") {
			parameter.getString("GuiTask", "inPortRef", connections.guiTask.inPortRef);
			parameter.getInteger("GuiTask", "prescale", connections.guiTask.prescale);
		}
		if(parameter.checkIfParameterExists("GuiTask", "scheduler")) {
			parameter.getString("GuiTask", "scheduler", connections.guiTask.scheduler);
		}
		if(parameter.checkIfParameterExists("GuiTask", "priority")) {
			parameter.getInteger("GuiTask", "priority", connections.guiTask.priority);
		}
		if(parameter.checkIfParameterExists("GuiTask", "cpuAffinity")) {
			parameter.getInteger("GuiTask", "cpuAffinity", connections.guiTask.cpuAffinity);
		}
		// load parameters for task ImageTask
		parameter.getDouble("ImageTask", "minActFreqHz", connections.imageTask.minActFreq);
		parameter.getDouble("ImageTask", "maxActFreqHz", connections.imageTask.maxActFreq);
		parameter.getString("ImageTask", "triggerType", connections.imageTask.trigger);
		if(connections.imageTask.trigger == "PeriodicTimer") {
			parameter.getDouble("ImageTask", "periodicActFreqHz", connections.imageTask.periodicActFreq);
		} else if(connections.imageTask.trigger == "DataTriggered") {
			parameter.getString("ImageTask", "inPortRef", connections.imageTask.inPortRef);
			parameter.getInteger("ImageTask", "prescale", connections.imageTask.prescale);
		}
		if(parameter.checkIfParameterExists("ImageTask", "scheduler")) {
			parameter.getString("ImageTask", "scheduler", connections.imageTask.scheduler);
		}
		if(parameter.checkIfParameterExists("ImageTask", "priority")) {
			parameter.getInteger("ImageTask", "priority", connections.imageTask.priority);
		}
		if(parameter.checkIfParameterExists("ImageTask", "cpuAffinity")) {
			parameter.getInteger("ImageTask", "cpuAffinity", connections.imageTask.cpuAffinity);
		}
		
		// load parameters for ComponentUnicapImageClientROS1InterfacesExtension
		
		// load parameters for ComponentUnicapImageClientROSExtension
		
		// load parameters for ComponentUnicapImageClientRestInterfacesExtension
		
		// load parameters for OpcUaBackendComponentGeneratorExtension
		
		// load parameters for PlainOpcUaComponentUnicapImageClientExtension
		
		
		// load parameters for all registered component-extensions
		for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
		{
			extension->second->loadParameters(parameter);
		}
		
		paramHandler.loadParameter(parameter);
	
	} catch (const SmartACE::IniParameterError & e) {
		std::cerr << e.what() << std::endl;
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std::exception: " << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}
