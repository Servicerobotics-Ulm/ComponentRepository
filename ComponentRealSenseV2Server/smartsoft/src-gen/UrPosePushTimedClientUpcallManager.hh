//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef _URPOSEPUSHTIMEDCLIENT_UPCALL_MANAGER_HH
#define _URPOSEPUSHTIMEDCLIENT_UPCALL_MANAGER_HH

#include <list>
#include "aceSmartSoft.hh"
#include "UrPosePushTimedClientUpcallInterface.hh"

/** UrPosePushTimedClientUpcallManager connects input-handling with Upcall propagation
 *
 * This class implements an InputHandler for the InputPort UrPosePushTimedClient and propagates the handling 
 * of incoming data to all associated (i.e. attached) Upcalls.
 */
class UrPosePushTimedClientUpcallManager
:	public Smart::IInputHandler<CommManipulatorObjects::CommMobileManipulatorState>
{
private:
	// list of associated updalls
	std::list<UrPosePushTimedClientUpcallInterface*> upcalls;

	// call the on_UrPosePushTimedClient of all the attached UrPosePushTimedClientUpcallInterfaces
	void notify_upcalls(const CommManipulatorObjects::CommMobileManipulatorState &input);
	
protected:
	virtual void handle_input(const CommManipulatorObjects::CommMobileManipulatorState &input) {
		// relay input-handling to all attached UrPosePushTimedClientUpcallInterfaces
		this->notify_upcalls(input);
	}
public:
	UrPosePushTimedClientUpcallManager(
		Smart::InputSubject<CommManipulatorObjects::CommMobileManipulatorState> *subject,
		const int &prescaleFactor=1
	);
	virtual ~UrPosePushTimedClientUpcallManager();
	
	void attach(UrPosePushTimedClientUpcallInterface *upcall);
	void detach(UrPosePushTimedClientUpcallInterface *upcall);
};

#endif
